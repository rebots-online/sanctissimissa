Troubleshoot verdict: two distinct causes.

1. **"Canned response" = designed mock engine, not a malfunction.** v1.45 ships CP.5 with a deterministic stand-in (`MockEngine`, `src/core/chat/session.ts:38-47`); the real on-device engine is unimplemented (CP.2 model store, CP.3 WebGPU TurboQuant, CP.4 registry — all unchecked in `CHECKLIST.md:2313-2328`). The screenshot's reply ("℣. Dóminus vobíscum. You bring ⟨…⟩ before the altar…") is exactly the mock formula; the AppImage is the correct v1.45.27008 build.
2. **Badge overlap = real bug.** `src/ui/ChatView.tsx:197` unmounts the badge only in `fullscreen` mode; in default `dock-right` the badge (fixed `right:18px; bottom:54px`, z-index 60) renders on top of the panel (z-index 55) over the input/Send area.

Chosen scope (recommended option, absent user answer): fix the badge bug + make the stand-in obviously a preview. The real engine stays a separate CP.2/CP.3/CP.4 effort.

## Changes

**A. Badge gets out of the way (bug fix)**
- `src/ui/ChatView.tsx:197` — change the render condition from `!(open && dock === 'fullscreen')` to `!open`, so the badge unmounts whenever the panel is open in **every** dock mode. Panel header's existing × button (`ChatView.tsx:226`) closes the panel and brings the badge back. `ChatBadge` component itself is untouched (its `open` prop simply is always false when rendered).

**B. Preview clarity (mock honesty pass)**
- `src/core/chat/session.ts` `MockEngine.reply()` — rotate through a small set of deterministic liturgical stand-in formulas (cycle by turn index, no RNG, first formula stays the current "Dóminus vobíscum" text so existing tests remain green). Replies stop reading as one broken canned string.
- `src/ui/ChatView.tsx` header — add a small "Preview engine" chip next to `<h3>Companion</h3>`, rendered only when `session.modelId.startsWith('mock://')` so it auto-disappears when the CP.3/CP.4 real engine plugs in.
- `src/styles.css` — style `.chat-engine-chip` in the existing house style (small, low-chrome, matching `.chat-modes` button styling conventions).

**C. Tests** (`tests/chatView.test.ts`, following its static-source assertion pattern)
- New: badge unmounts whenever panel is open (`chatView` includes `{!open && <ChatBadge`).
- New: mock engine rotates formulas across turns while staying deterministic; "Preview engine" chip is conditional on `mock://`.
- Existing 9 tests must stay green (first-formula rotation preserves the `Dóminus vobíscum` / `Introibo` assertions).

**D. Checklist note**
- Add one line under the CP.5 entry in `CHECKLIST.md` recording this hotfix (badge hide-all-modes + preview chip), per house discipline.

## Verification & ship
- Run the test suite (repo driver, concurrency 4).
- Visual check in a browser against the dev/web build: badge hidden while panel open in dock-right/dock-left/floating/inline; × restores badge; chip visible in header.
- Per standing incremental-ship rule: after green tests, commit + push to GitHub immediately, then run the standard release train (driver self-commits the version stamp; realtime output shown throughout) and re-test the fresh AppImage from `dist/`.
- Not in scope: CP.2/CP.3/CP.4 real-engine implementation, any Rust-side work (src-tauri has zero chat code by design).