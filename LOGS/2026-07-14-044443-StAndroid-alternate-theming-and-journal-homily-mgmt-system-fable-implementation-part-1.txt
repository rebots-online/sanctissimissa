 ▐▛███▜▌   Claude Code v2.1.208
▝▜█████▛▘  Fable 5 · Claude Max
  ▘▘ ▝▝    ~/CascadeProjects/StAndroidsMissal

 ▎ Extended through July 19
 ▎ We're extending Claude Fable 5 access on all paid plans, as well as keeping Claude Code’s weekly rate limits 50% higher, through July 19.
 ▎
 ▎ As before, you can use up to half of your weekly usage limit on Fable 5. After that, you can keep using Fable 5 with usage credits, or switch to another model to keep working within your remaining limits.
 ▎
 ▎ More details here: https://support.claude.com/en/articles/15424964-claude-fable-5-promotional-access
   +1 more · /status

❯ /model                                                                        
  ⎿  Set model to Fable 5 and saved as your default for new sessions

❯  Can you create an alternate theming based on the look and feel of            
  @DOCS/Sanctissimissa-Template.html You may retain                             
    the background colouring to match the rest of the framework. For mobile     
  views, the current stacking sections is no                                    
    good--it will cause difficulty followineg when having to scroll a whole     
  section at a  time; instead, when hasving to                                  
    render on narrow screens that can't reasonably accommdate two  full         
  colummns, switch to an alternating Latin first                                
    in bold and English inset alternating row, indented italics,                
  lighter-coloured (de-emphasized) then blank linel then                        
    back to darker main Latin; selecting with cursor would then highlight both  
  Latin and English equivalent                                                  
    simultaneously. The vector similarity search has to be much improved; as it 
  is now, with raw overwhleming                                                 
    references, itis not really useful--it might be usefuk to show a small      
  icon-sized relative vector mapping to show                                    
    relatively how semantically-related relatve to other usages and relate to   
  whole passages atomically at least a                                          
    semantically-useful clause of a sentence to be useful, or                   
  imagery/metaphor/allegorical/seminal reference such as the                    
    repeating metaphors across old testament, new testament, psalms, etc. For   
  navigation in the bible study/reader,                                         
    orgfanize the table of contents more aloneg the lines of imagery or         
  scenarios, for Gospels, emphsasize the parallels                              
    and complementarities--don't be afraid to break the columnar rigid for      
  differentially-sized fonts (almost word                                       
    cloud nature) and point out a logical way to make the navigation            
  meaningful, more importantly even than ordinal                                
    arrangement. For the journalling, see                                       
  @DOCS/standroids-journal-sidecar-standalone.html and build in the journalling 
  and Homily Management System interface, per @DOCS/StAndroidsMissal-Additiona  
  l-PRD-pieces_copilot_message_export_july_14_2026_1_50am.md                    
  ⎿  Read DOCS/StAndroidsMissal-Additional-PRD-pieces_copilot_message_export_jul
     y_14_2026_1_50am.md (131 lines)

● I'll start by following the mandatory session bootstrap (/sesh resume per
  §0.5), and read the two referenced design documents.

● Skill(sesh)
  ⎿  Successfully loaded skill

  Searched for 2 patterns, read 4 files, listed 1 directory, called codegraph 2 times, ran 2 shell commands (ctrl+o to expand)

● Updated plan
  ⎿  /plan to preview

● User answered Claude's questions:
  ⎿  · This is a large effort (6 workstreams). Which should this pass actually 
     implement? (Contract amendments happen first regardless.) → All six 
     (Recommended),  Remember, design it to be architected so it can all be 
     realized and then coded in parallel so it's not actually that huge of an 
     effort effectively 
     · Should the new Sanctissimissa-derived theme become the default look, or a
     selectable alternate with the current parchment skeuomorphic staying 
     default? → Selectable alternate (Recommended)
     · Where should Gospel parallels data come from? (Corpus-sovereignty rule: 
     nothing references outside the repo.) → Try to find and vendor 
     in/ingest/vector embed the public domain resources such as those in 
     @DOCS/ScripturalReferences-PublicDomain.md

  Read 1 file, listed 1 directory (ctrl+o to expand)

● Updated plan
  ⎿  /plan to preview
● User approved Claude's plan
  ⎿  Plan saved to: ~/.claude/plans/can-you-create-an-jolly-simon.md · /plan to 
     edit
     St. Android's Missal — Sanctissima theme, mobile interleave, similarity UX,
     Scripture Atlas, Journal/Homily sidecar

     Context

     The operator asked for five coupled improvements, confirmed all in scope,
     architected so the work fans out to parallel coders after one contract
     amendment:

     1. Alternate theming modeled on DOCS/Sanctissimissa-Template.html —
     selectable alternate (current parchment skeuomorphic stays default). Retain
     existing background colouring; adopt the template's white elevated cards,
     accordion idiom, gold-tinted propers, red rubric/priest + purple server
     dialogue colouring, Crimson-Pro-like serif feel.
     2. Mobile bilingual interleave: on narrow screens, replace stacked
     full-section columns with alternating rows — Latin bold first, English
     inset/italic/de-emphasized beneath, blank line between pairs; cursor
     selection highlights both languages simultaneously.
     3. Vector similarity made useful: clause-level atomicity, icon-sized
     relative "vector map" glyph per hit, grouping by imagery/metaphor/allegory
     across OT/NT/Psalms.
     4. Scripture TOC by imagery/scenario; Gospels emphasize
     parallels/complementarities; differential (word-cloud-like) font sizing;
     meaning-first over ordinal navigation.
     5. Journalling + Homily Management System per
     DOCS/standroids-journal-sidecar-standalone.html prototype and the Pieces
     PRD (DOCS/StAndroidsMissal-Additional-PRD-…july_14_2026….md).

     Gospel-parallels / interpretive data source (operator decision): vendor the
     public-domain Catholic sources from
     DOCS/ScripturalReferences-PublicDomain.md — this pass vendors the two
     verse-keyed, parallels-bearing ones (Haydock 1859 — already stanza B-F —
     and Aquinas, Catena Aurea, Newman tr.), ingests them as a generic
     verse-keyed interpretive layer, and records the remaining 13 sources as
     roadmap stanzas over the same schema (zero schema change per added source).

     Contract state (verified): DOCS/ARCHITECTURE.md v0.4 already specifies much
     of this:
     - Phase E [ ]: semantic tokens (--surface --surface-2 --ink --ink-soft 
     --accent --pane-latin-bg --pane-english-bg --rail-bg --card-border),
     html[data-theme=…][data-mode=…], src/core/theme/themes.ts
     (ThemeFamily/applyTheme/systemMode), ThemePicker; seasonal data-color
     orthogonal.
     - §7.6 Accompaniment plane, stanzas B-C/B-D [ ]: one Accompaniment object,
     four exposures, SIDECAR_SCHEMA_SQL_V2/SidecarDb v2 (sql.js; IndexedDB web /
     Tauri load_sidecar/save_sidecar), occurrence selectors via computus,
     AccompanimentEditor (TipTap), JournalView, HomilyPlanner,
     sidecar_embeddings+embedText. The PRD/prototype are the UI elaboration of
     these rows.
     - B-F.1 [ ]: Haydock vendoring (clone-at-home, PROVENANCE.md lock BEFORE
     assimilation — INC-15).

     Key code facts (verified via codegraph):
     - src/styles.css (486 lines, single sheet): raw palette vars +
     html[data-color=…]; .bilingual = grid 1fr 1fr → stacks whole columns
     ≤1100px (the complaint).
     - src/ui/ReaderView.tsx: line-parallel corpus; TextBlock renders
     span[data-line]; .xlate-echo already highlights the aligned line in the
     other pane on hover/selectionchange; alignSelection/wordEcho in
     src/core/text/align.ts; ctx-menu has Meaning/Similar/Cross-refs/Annotate.
     - src/ui/MeaningPanel.tsx: db.groupedConcordance / db.groupedSimilarToText
     concept-grouped hits.
     - src/core/ontology/concepts.ts: curated taxonomy → concept nodes at
     ingest; src/core/vector/embed.ts: deterministic 128-d hashed-trigram
     embedText (cheap at runtime).
     - src/ui/BibleView.tsx +
     CorpusDb.getBooks/getChapter/citationsOf/liturgyCitingChapter; 32,830 CITES
     edges = free liturgical-prominence weights.
     - src/App.tsx: View/NAV rail; hash routes; #/acc/ reserved (BB.3
     remainder).

     Phase 1 — Contract amendment (serial, gates everything)

     Amend DOCS/ARCHITECTURE.md (+ re-attest) and append CHECKLIST stanzas with
     self-contained, I-4-grade tasks (each names files, entities, signatures,
     Verify/Accept/Commit) so Phase 2 fans out to parallel coders:

     New/updated entity rows:
     - ThemeFamily gains 'sanctissima'; --rubric/--dialogue-p/--dialogue-s
     tokens; dialogue/rubric line-prefix detection in TextBlock.
     - Interleaved bilingual mode: TextBlock paired-render,
     .bilingual-interleaved CSS, echo-range extension.
     - SimilarityGlyph component; bestClause(text, query) runtime clause focus
     (src/core/vector/clause.ts); imagery-concept extension of concepts.ts (~15
     imagery/metaphor entries with seed phrases).
     - ScriptureAtlas nav modes in BibleView; src/core/ontology/parallels.ts
     pericope-parallel table (curated spine, sourced/checked against Catena
     Aurea while ingesting).
     - Interpretive layer (generalizes B-F): commentary_blocks verse-range-keyed
     nodes (kind='commentary', meta.source, embeddings) fed by any
     VENDORED/<source>/; sources roadmap = the 15-row PD table; this pass ships
     Haydock + Catena Aurea.
     - Sidecar workspace UI entities refining B-D per prototype: capture flow,
     sjp-related-style connections panel (why-bridge + evidence chips),
     destinations (journal/seed/theme/schedule), toast.

     Phase 2 — Parallel workstreams (dependency graph)

     W1 sidecar v2 (BC.1/2/4) ──► W4 journal/HMS workspace (BC.3, BD.1/2, PRD)
     W2 theme system + sanctissima          (independent)
     W3 mobile interleave + dual-selection  (independent)
     W5 similarity UX (clause+glyph)        (independent; imagery concepts →
     shared re-ingest)
     W6 vendoring + interpretive ingest ──► Scripture Atlas modes (shared
     re-ingest with W5)
     One re-ingest lands W5's imagery concepts + W6's commentary in a single
     assets/missal.db regeneration (LFS on forgejo.robin.mba; fill-log delta
     must be zero).

     W1 — Sidecar v2 foundation (BC.1, BC.2, BC.4 — already fully specified 
     rows)

     src/core/accompaniment/types.ts (Accompaniment, OccurrenceSelector,
     Exposure); store.ts (SIDECAR_SCHEMA_SQL_V2, SidecarDb v2 — web IndexedDB
     blob, Tauri load_sidecar/save_sidecar commands in src-tauri/src/lib.rs);
     localStorage-annotations migration (old key read-only); resolve.ts
     (accompanimentsForDay/forAnchor/matchesSelector over
     resolveDay/dateForWeekKey); tests/accompaniment.test.ts (node:sqlite
     headless). Settings rows here serve W2's theme persistence and W4's role
     lens.

     W2 — Theme system + sanctissima (E1 + token refactor + family)

     - themes.ts per E1 spec + 'sanctissima'; applyTheme → data-theme/data-mode;
     systemMode().
     - Mechanical token refactor of styles.css (--parchment→--surface,
     --vellum→--surface-2, --parchment-deep→--card-border, --rail→--rail-bg);
     default family = current values, zero visual change.
     - sanctissima block: parchment background retained; content in white 
     elevated cards (shadow, 12px radius), accordion heads styled on the
     existing fold/unfold, propers gold-left-border + warm tint, --rubric red +
     dialogue P/V red · S/R purple (prefix detection benefits every family),
     serif stack biased Crimson-Pro-like (bundled/local only). Light + dark
     variants.
     - ThemePicker in rail; persists theme.family/theme.mode to sidecar settings
     (localStorage fallback until W1 merges).

     W3 — Mobile interleaved bilingual

     - TextBlock paired-render: single interleaved column when narrow (≤1100px,
     replacing today's stack): per line i — Latin bold (--ink), English beneath
     indented ~1.1em, italic, --ink-faint; gap before next pair. NULL English →
     Latin-only row.
     - Selection sync: extend the existing selectionchange single-line echo to
     the full line-range of the selection — every selected Latin line lights its
     paired English row (.xlate-echo) and vice versa. Same pattern in BibleView
     (verse = pair unit).
     - No data changes; render + CSS + echo-range only.

     W4 — Journal & Homily sidecar workspace (after W1)

     - AccompanimentEditor: TipTap (@tiptap/react, @tiptap/starter-kit; stores
     body_pm+body_html); toolbar, title, tags, bilingual source-quote block,
     save-state, toast.
     - Capture flow (PRD §2.1): ctx-menu gains "✎ Add to Journal/Homily notes"
     and "🖍 Highlight both panes"; capture opens sidecar pane (split,
     MeaningPanel pattern) pre-filled with quote + aligned counterpart
     (alignSelection), anchor, timestamp. Highlight = lightweight accompaniment
     rendered via mark.ann in both panes (quote/quoteAlt already supported).
     - Connections panel (prototype sjp-related): corpus vector hits
     (groupedSimilarToText over note+quote) + user's past accompaniments
     (runtime embedText → sidecar_embeddings) + commentary blocks once W6 lands;
     cards carry why-bridge + evidence chips + add/dismiss.
     - Destinations → exposure + OccurrenceSelector (journal | homily seed |
     theme/series | schedule).
     - JournalView (date timeline) + HomilyPlanner (selector-projected calendar)
     under new rail view "Journal & Homilies" (View+NAV+#/acc/ route,
     completing BB.3). Priest/laity vocabulary from settings mode.
     - Voice dictation + attachments: stretch only.

     W5 — Similarity UX (MeaningPanel)

     - bestClause: split hit text on .:;· (min length), embedText each vs query,
     cosine-argmax → render best clause emphasized, remainder collapsed behind
     "more".
     - SimilarityGlyph: icon-sized SVG radial mini-map — query at center, this
     hit's dot at distance ∝ (1−score), sibling hits ghosted; numeric score
     demoted to tooltip.
     - Imagery concepts (~15: Light/Darkness, Shepherd, Vine, Water/Baptism,
     Bread, Lamb, Kingship, Bridegroom, Desert/Exile, Mountain, Temple,
     Sacrifice, Harvest, Way, Rock) added to concepts.ts → grouping spans
     OT/NT/Psalms after the shared re-ingest.

     W6 — PD vendoring + interpretive layer + Scripture Atlas

     - Vendor Haydock (fulfils B-F.1) and Catena Aurea into VENDORED/<source>/:
     clone-at-home, PROVENANCE.md (remote+tag+commit+date) locked before
     assimilation, per INC-15; additive, nothing deleted.
     - Ingest pass: verse-range-keyed commentary_blocks nodes + embeddings +
     FTS; read-only commentary layer in BibleView verse view; vendored
     provenance surfaces in W4's connections panel.
     - parallels.ts: curated pericope-parallel spine (~60 pericopes ×
     Mt/Mk/Lk/Jn), cross-checked against Catena Aurea during ingest; embedding
     complementarities fill uncovered stretches.
     - Scripture Atlas: BibleView nav modes — Canonical (existing) · Imagery & 
     scenarios (imagery concepts as differential-size label field, font-size ∝
     √(linked-verse count), grouped into scenario clusters: Creation & Fall,
     Exodus & Desert, Kingdom & Exile, Passion, Resurrection…; tap → verse
     ranges grouped OT/Psalms/NT, CITES-weighted) · Gospel parallels (aligned
     pericope rows, font weight ∝ CITES count, click-through #/verse/…).

     Verification

     - npm test green throughout (new: accompaniment, parallels, bestClause
     tests; verse-count tests unchanged).
     - Browser pass per workstream (npm run dev): theme toggle families × modes
     × seasonal colors; ≤700px window shows interleave with dual-language
     selection highlight; capture→edit→connect→destination round-trip persists
     across reload; MeaningPanel clause-emphasized hits + glyphs; Atlas modes
     navigate to verses; commentary renders under verses with source
     attribution.
     - Re-ingest once: fill-log delta reviewed (zero new fill rows); db
     regenerated via LFS to forgejo.robin.mba.
     - Per-task commits (I-10(c)/I-18); markers [ ]→[X] on semantic
     verification, ✅ after running; push origin + github mirror.

     Out of scope (this pass)

     Remaining 5 theme-family token blocks (E2 pending tasks),
     NewsletterDesk/StudyBuilder (BD.3/4), Companion (B-I), the other 13 PD
     sources (roadmap stanzas over the same interpretive-layer schema),
     voice/attachments beyond stretch, Android widget.

  Read 1 file (ctrl+o to expand)

● Phase 1 begins: amending DOCS/ARCHITECTURE.md with §7.7 (the new presentation
  & meaning plane), entity rows, decision updates, and re-attestation.

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 1 line, removed 1 line
      31  10. **Two-plane memory.** `missal.db` is canonical, read-only, 
          regenerated only by ingest. All user data lives in a **separate
           sidecar SQLite** (`SidecarDb`) whose every row carries `id` (u
          uid) / `device_id` / `updated_at` / `deleted_at` (tombstone) so
           multi-device + parish-group sync can be layered on without sch
          ema change. The two planes never mix; the sidecar never stores 
          corpus text, only `nodeKey`/liturgical-key anchors.
      32  11. **Liturgical-key anchoring for recyclable content.** Homili
          es anchor to the *liturgical* key (`weekKey` or `Sancti/MM-DD` 
          feast key), not the civil date, with optional per-year overlay 
          rows (`year` column; `year IS NULL` = the base homily). Content
           recycles annually by construction.
      33  12. **One shared bilingual renderer.** `SectionReader` (extract
          ed from `ReaderView`) renders `ReaderEntry[]` for both Mass and
           Office modes — annotations, selection menu, and exegesis machi
          nery are written once.
      34 -13. **Semantic theme tokens.** All component CSS consumes seman
         -tic tokens (`--surface`, `--surface-2`, `--ink`, `--ink-soft`, 
         -`--accent`, `--pane-latin-bg`, `--pane-english-bg`, `--rail-bg`
         -, `--card-border`); a theme is a `data-theme` (family) + `data-
         -mode` (light|dark) pair on `<html>`; the seasonal liturgical ac
         -cent (`data-color`) stays orthogonal. Six families × two modes 
         -= twelve themes as pure token blocks — no component edits per t
         -heme.                                                          
      34 +13. **Semantic theme tokens.** All component CSS consumes seman
         +tic tokens (`--surface`, `--surface-2`, `--ink`, `--ink-soft`, 
         +`--accent`, `--pane-latin-bg`, `--pane-english-bg`, `--rail-bg`
         +, `--card-border`); a theme is a `data-theme` (family) + `data-
         +mode` (light|dark) pair on `<html>`; the seasonal liturgical ac
         +cent (`data-color`) stays orthogonal. Seven families × two mode
         +s = fourteen themes as pure token blocks — no component edits p
         +er theme (the seventh, `sanctissima`, is the Sanctissimissa-tem
         +plate-derived card family, §7.7).                              
      35  14. **Lore is hand-authored data, not fetched.** Station/line l
          ore ships as a typed constant module (`stationLore.ts`); no net
          work, no LLM in this phase (decision 6 applies — no fabricated 
          liturgical claims at runtime).
      36  15. **Entitlements: RevenueCat authoritative, gates are data.**
           The client asks only RevenueCat (key via `VITE_REVENUECAT_API_
          KEY`, never hardcoded); BTCPay/WooCommerce sync *into* RevenueC
          at via a server-side bridge specified in `DOCS/ENTITLEMENT-SYNC
          .md` (interface spec; separate deployable, not this repo's code
          ). `FEATURE_GATES` maps feature → required entitlement or `null
          ` (ungated); v0.2 ships all-`null` (G3) so deciding tiers later
           edits one map.
      37  16. **Deep links are URL params, parsed once at boot.** `?view=
          &date=&section=&quote=` — `parseDeepLink` feeds initial App sta
          te; share payloads embed the same URL.

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 16 lines
      205  
      206  **Android widget:** Kotlin `AppWidgetProvider` in `src-tauri/g
           en/android` — today's feast + daily reading refs, deep-link in
           tent (hash route); data JSON maintained by the app + daily ref
           resh. Web = PWA shortcuts; desktop skipped in v1.
      207  
      208 +## 7.7 Presentation & meaning plane (v0.5 — sanctissima theme,
          + interleaved bilingual, similarity UX, Scripture Atlas, interp
          +retive layer, journal sidecar workspace)                      
      209 +                                                              
      210 +**Mandate (operator, 2026-07-14):** five coupled presentation/
          +meaning upgrades — an alternate theme derived from `DOCS/Sanct
          +issimissa-Template.html`; a mobile-usable interleaved bilingua
          +l layout with dual-language selection; vector similarity made 
          +*useful* (clause atomicity, relative-distance glyph, imagery g
          +rouping); meaning-first Scripture navigation (imagery/scenario
          +s, Gospel parallels, differential sizing); and the Journal/Hom
          +ily Management workspace per `DOCS/standroids-journal-sidecar-
          +standalone.html` + the 2026-07-14 PRD (`DOCS/StAndroidsMissal-
          +Additional-PRD-pieces_copilot_message_export_july_14_2026_1_50
          +am.md`). Architected for parallel coder dispatch: one shared-f
          +ile owner per wave (open question 8 amended below).           
      211 +                                                              
      212 +**Sanctissima theme family (extends decision 13):** `ThemeFami
          +ly` gains `'sanctissima'`. Background tokens retain the parchm
          +ent framework (operator: keep the framework's background colou
          +ring); content surfaces render as **elevated white cards** (`-
          +-card`, `--card-shadow`, 12px radii), accordion section heads 
          +on the existing fold/unfold affordance, propers sections carry
          + a gold left border + warm tint. New **liturgical text-role to
          +kens consumed by every family**: `--rubric` (rubric red), `--d
          +ialogue-p` (priest/versicle voice), `--dialogue-s` (server/res
          +ponse voice). Line-prefix detection (`dialogueClass`, `src/cor
          +e/text/dialogue.ts`) is rendering-level only — stored corpus t
          +ext is never modified: `V.`/`℣.`/leading `P.` → dialogue-p; `R
          +.`/`℟.`/leading `S.` → dialogue-s. Selectable alternate: `DEFA
          +ULT_FAMILY` stays `'skeuomorphic'`; both families ship light +
          + dark. Fonts stay bundled-local (the template's Crimson-Pro fe
          +el = local serif stack bias, no remote fonts). The template's 
          +translation-popup idiom is already served by the shipped word-
          +callout/echo grammar — not duplicated.                        
      213 +                                                              
      214 +**Interleaved bilingual mode:** below 1100px (and later by use
          +r preference `reader.layout`), the two-pane grid is replaced b
          +y a single interleaved column rendered by the shared renderer 
          +`BilingualText` (`src/ui/BilingualText.tsx`, extracted from Re
          +aderView's internal TextBlock; adopted by ReaderView, OfficeVi
          +ew-via-SectionReader when P-B lands, and BibleView at verse gr
          +anularity): per aligned line *i* — Latin first (**bold**, `--i
          +nk`), its English beneath (indented 1.1em, italic, `--ink-fain
          +t`), then a gap before the next pair; NULL English → Latin-onl
          +y row. The existing `selectionchange` single-line echo extends
          + to the **full line-range** of the live selection, lighting ev
          +ery counterpart row (`.xlate-echo`) in both directions — the c
          +ursor effectively selects both languages simultaneously. CSS b
          +lock `.bilingual-interleaved`.                                
      215 +                                                              
      216 +**Similarity UX (MeaningPanel):** each similar-hit gains (a) *
          +*clause focus** — `bestClause(text, query)` (`src/core/vector/
          +clause.ts`) splits on `.:;·` boundaries (min 25 chars/clause),
          + embeds clauses via the existing `embedText`, cosine-argmax; t
          +he winning clause renders emphasized with the remainder collap
          +sed behind "more" — hits become quotable at semantic-clause gr
          +anularity; (b) **`SimilarityGlyph`** (`src/ui/SimilarityGlyph.
          +tsx`) — an icon-sized radial SVG: query at center, this hit's 
          +dot at radius ∝ (1−score), sibling hits ghosted, so relative r
          +elatedness is visible at a glance; the raw score demotes to a 
          +tooltip; (c) **imagery grouping** — `IMAGERY_CONCEPTS` (~15 im
          +agery/metaphor/typology concepts with seed phrases: Light & Da
          +rkness, Shepherd & Flock, Vine & Vineyard, Water & Baptism, Br
          +ead from Heaven, Lamb & Sacrifice, King & Kingdom, Bridegroom 
          +& Bride, Desert & Exile, Mountain of God, Temple & Dwelling, H
          +arvest & Vintage, The Way, Rock & Foundation, Fire & Spirit) m
          +erged into `concepts.ts` and ingested exactly like the existin
          +g taxonomy, so `INSTANCE_OF` grouping spans OT/Psalms/NT throu
          +gh the verse nodes.                                           
      217 +                                                              
      218 +**Scripture Atlas (BibleView navigation modes):** `AtlasMode =
          + 'canonical' | 'imagery' | 'parallels'` — meaning-first naviga
          +tion is additive; canonical grids remain. *Imagery mode:* imag
          +ery concepts as a differential-size label field (font-size ∝ √
          +(linked verse count); liturgical prominence via CITES counts) 
          +grouped under hand-authored `SCENARIO_CLUSTERS` (Creation & Fa
          +ll, Exodus & Desert, Kingdom & Exile, Wisdom & Psalter, Incarn
          +ation, Public Ministry, Passion, Resurrection & Church); tappi
          +ng a label lists its verse ranges grouped OT/Psalms/NT. *Paral
          +lels mode:* `PERICOPES` (`src/core/ontology/parallels.ts`, cur
          +ated ~60-pericope spine × Mt/Mc/Lc/Jo columns, cross-checked a
          +gainst the vendored Catena Aurea) as aligned rows, font weight
          + ∝ CITES count, click-through `#/verse/…`; chapter-level embed
          +ding cosine supplies "complementarities" between untabled stre
          +tches.                                                        
      219 +                                                              
      220 +**Interpretive layer (generalizes the §7.6 Haydock row):** any
          + public-domain source vendored under `VENDORED/<source>/` (clo
          +ne-at-home; PROVENANCE.md lock **before** assimilation — INC-1
          +5; additive, nothing deleted) ingests via `scripts/ingest-comm
          +entary.mjs` into the existing graph tables: nodes `kind='comme
          +ntary'`, key `commentary:<source>/<Book>/<ch>/<verse>`, `meta.
          +source`, English `text_blocks` (PD translations are English; t
          +he Latin column stays NULL — decision 5 governs liturgical tex
          +t, not commentary), embeddings + FTS rows, `COMMENTS_ON` edges
          + to verse nodes. Query surface `CorpusDb.commentaryFor(book, c
          +h, verse?)`. **This wave ships Haydock 1859** (fulfils the §7.
          +6 `VENDORED/haydock/` row) **and Catena Aurea** (Newman tr. 18
          +41–45); the remaining 13 sources of `DOCS/ScripturalReferences
          +-PublicDomain.md` (Augustine on the Psalms, Bellarmine, à Lapi
          +de, Guéranger, Bute Breviary, Roman Catechism, Summa, Ignatius
          +, John of the Cross, de Sales, Imitation, Catholic Encyclopedi
          +a, DR/Vulgate whole) are the roadmap over this same schema — z
          +ero schema change per added source. BibleView renders commenta
          +ry read-only beneath verses with source attribution; the conne
          +ctions panel surfaces commentary as `vendored` evidence.      
      221 +                                                              
      222 +**Journal sidecar workspace (UI elaboration of §7.6 B-C/B-D pe
          +r the prototype + PRD):** reader context menus (ReaderView, Bi
          +bleView, OfficeView once SectionReader lands) gain **"✎ Add to
          + Journal/Homily notes"** and **"🖍 Highlight both panes"**. Cap
          +ture opens `JournalSidecar` (`src/ui/JournalSidecar.tsx`, righ
          +t split pane on the MeaningPanel pattern): source block (bilin
          +gual quote via `alignSelection` + anchor nodeKey/verse ref + c
          +apture timestamp), embedded `AccompanimentEditor`, `Connection
          +sPanel` (corpus vector hits via `groupedSimilarToText` over no
          +te+quote, the user's own past accompaniments via runtime `embe
          +dText` against `sidecar_embeddings`, and commentary blocks; ev
          +ery card carries a why-bridge line + evidence chips + add-as-s
          +ource/dismiss — routes, not bare similarity scores), a destina
          +tions row mapping to `exposure` + `OccurrenceSelector` (keep a
          +s journal | promote to homily seed | attach to theme/series | 
          +schedule for a liturgical occasion), and toast feedback. Dual-
          +pane highlight = lightweight accompaniment (quote + quoteAlt) 
          +rendered through the existing `mark.ann` pipeline in **both** 
          +panes. Priest/laity vocabulary from settings `mode`. Voice dic
          +tation and attachments: schema-ready (`attachments` JSON per P
          +RD), out of scope this wave.                                  
      223 +                                                              
      224  ## 8. Entity Table
      225  
      226  Status: **S** = shipped (on disk now) · **P-<phase>** = planne
           d, target location. Independent agents must produce these iden
           tifiers byte-identically.

❯ also, if there are references to 'Sanctissima' or "sanctissima missa" they    
  are typos; we use "Sanctissimissa" as in the portmanteau                      

  Searched for 1 pattern (ctrl+o to expand)

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 1 line, removed 1 line
      31  10. **Two-plane memory.** `missal.db` is canonical, read-only, 
          regenerated only by ingest. All user data lives in a **separate
           sidecar SQLite** (`SidecarDb`) whose every row carries `id` (u
          uid) / `device_id` / `updated_at` / `deleted_at` (tombstone) so
           multi-device + parish-group sync can be layered on without sch
          ema change. The two planes never mix; the sidecar never stores 
          corpus text, only `nodeKey`/liturgical-key anchors.
      32  11. **Liturgical-key anchoring for recyclable content.** Homili
          es anchor to the *liturgical* key (`weekKey` or `Sancti/MM-DD` 
          feast key), not the civil date, with optional per-year overlay 
          rows (`year` column; `year IS NULL` = the base homily). Content
           recycles annually by construction.
      33  12. **One shared bilingual renderer.** `SectionReader` (extract
          ed from `ReaderView`) renders `ReaderEntry[]` for both Mass and
           Office modes — annotations, selection menu, and exegesis machi
          nery are written once.
      34 -13. **Semantic theme tokens.** All component CSS consumes seman
         -tic tokens (`--surface`, `--surface-2`, `--ink`, `--ink-soft`, 
         -`--accent`, `--pane-latin-bg`, `--pane-english-bg`, `--rail-bg`
         -, `--card-border`); a theme is a `data-theme` (family) + `data-
         -mode` (light|dark) pair on `<html>`; the seasonal liturgical ac
         -cent (`data-color`) stays orthogonal. Seven families × two mode
         -s = fourteen themes as pure token blocks — no component edits p
         -er theme (the seventh, `sanctissima`, is the Sanctissimissa-tem
         -plate-derived card family, §7.7).                              
      34 +13. **Semantic theme tokens.** All component CSS consumes seman
         +tic tokens (`--surface`, `--surface-2`, `--ink`, `--ink-soft`, 
         +`--accent`, `--pane-latin-bg`, `--pane-english-bg`, `--rail-bg`
         +, `--card-border`); a theme is a `data-theme` (family) + `data-
         +mode` (light|dark) pair on `<html>`; the seasonal liturgical ac
         +cent (`data-color`) stays orthogonal. Seven families × two mode
         +s = fourteen themes as pure token blocks — no component edits p
         +er theme (the seventh, `sanctissimissa`, is the Sanctissimissa-
         +template-derived card family, §7.7).                           
      35  14. **Lore is hand-authored data, not fetched.** Station/line l
          ore ships as a typed constant module (`stationLore.ts`); no net
          work, no LLM in this phase (decision 6 applies — no fabricated 
          liturgical claims at runtime).
      36  15. **Entitlements: RevenueCat authoritative, gates are data.**
           The client asks only RevenueCat (key via `VITE_REVENUECAT_API_
          KEY`, never hardcoded); BTCPay/WooCommerce sync *into* RevenueC
          at via a server-side bridge specified in `DOCS/ENTITLEMENT-SYNC
          .md` (interface spec; separate deployable, not this repo's code
          ). `FEATURE_GATES` maps feature → required entitlement or `null
          ` (ungated); v0.2 ships all-`null` (G3) so deciding tiers later
           edits one map.
      37  16. **Deep links are URL params, parsed once at boot.** `?view=
          &date=&section=&quote=` — `parseDeepLink` feeds initial App sta
          te; share payloads embed the same URL.

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 1 line, removed 1 line
      209  
      210  **Mandate (operator, 2026-07-14):** five coupled presentation/
           meaning upgrades — an alternate theme derived from `DOCS/Sanct
           issimissa-Template.html`; a mobile-usable interleaved bilingua
           l layout with dual-language selection; vector similarity made 
           *useful* (clause atomicity, relative-distance glyph, imagery g
           rouping); meaning-first Scripture navigation (imagery/scenario
           s, Gospel parallels, differential sizing); and the Journal/Hom
           ily Management workspace per `DOCS/standroids-journal-sidecar-
           standalone.html` + the 2026-07-14 PRD (`DOCS/StAndroidsMissal-
           Additional-PRD-pieces_copilot_message_export_july_14_2026_1_50
           am.md`). Architected for parallel coder dispatch: one shared-f
           ile owner per wave (open question 8 amended below).
      211  
      212 -**Sanctissima theme family (extends decision 13):** `ThemeFami
          -ly` gains `'sanctissima'`. Background tokens retain the parchm
          -ent framework (operator: keep the framework's background colou
          -ring); content surfaces render as **elevated white cards** (`-
          --card`, `--card-shadow`, 12px radii), accordion section heads 
          -on the existing fold/unfold affordance, propers sections carry
          - a gold left border + warm tint. New **liturgical text-role to
          -kens consumed by every family**: `--rubric` (rubric red), `--d
          -ialogue-p` (priest/versicle voice), `--dialogue-s` (server/res
          -ponse voice). Line-prefix detection (`dialogueClass`, `src/cor
          -e/text/dialogue.ts`) is rendering-level only — stored corpus t
          -ext is never modified: `V.`/`℣.`/leading `P.` → dialogue-p; `R
          -.`/`℟.`/leading `S.` → dialogue-s. Selectable alternate: `DEFA
          -ULT_FAMILY` stays `'skeuomorphic'`; both families ship light +
          - dark. Fonts stay bundled-local (the template's Crimson-Pro fe
          -el = local serif stack bias, no remote fonts). The template's 
          -translation-popup idiom is already served by the shipped word-
          -callout/echo grammar — not duplicated.                        
      212 +**Sanctissimissa theme family (extends decision 13):** `ThemeF
          +amily` gains `'sanctissimissa'`. Background tokens retain the 
          +parchment framework (operator: keep the framework's background
          + colouring); content surfaces render as **elevated white cards
          +** (`--card`, `--card-shadow`, 12px radii), accordion section 
          +heads on the existing fold/unfold affordance, propers sections
          + carry a gold left border + warm tint. New **liturgical text-r
          +ole tokens consumed by every family**: `--rubric` (rubric red)
          +, `--dialogue-p` (priest/versicle voice), `--dialogue-s` (serv
          +er/response voice). Line-prefix detection (`dialogueClass`, `s
          +rc/core/text/dialogue.ts`) is rendering-level only — stored co
          +rpus text is never modified: `V.`/`℣.`/leading `P.` → dialogue
          +-p; `R.`/`℟.`/leading `S.` → dialogue-s. Selectable alternate:
          + `DEFAULT_FAMILY` stays `'skeuomorphic'`; both families ship l
          +ight + dark. Fonts stay bundled-local (the template's Crimson-
          +Pro feel = local serif stack bias, no remote fonts). The templ
          +ate's translation-popup idiom is already served by the shipped
          + word-callout/echo grammar — not duplicated.                  
      213  
      214  **Interleaved bilingual mode:** below 1100px (and later by use
           r preference `reader.layout`), the two-pane grid is replaced b
           y a single interleaved column rendered by the shared renderer 
           `BilingualText` (`src/ui/BilingualText.tsx`, extracted from Re
           aderView's internal TextBlock; adopted by ReaderView, OfficeVi
           ew-via-SectionReader when P-B lands, and BibleView at verse gr
           anularity): per aligned line *i* — Latin first (**bold**, `--i
           nk`), its English beneath (indented 1.1em, italic, `--ink-fain
           t`), then a gap before the next pair; NULL English → Latin-onl
           y row. The existing `selectionchange` single-line echo extends
            to the **full line-range** of the live selection, lighting ev
           ery counterpart row (`.xlate-echo`) in both directions — the c
           ursor effectively selects both languages simultaneously. CSS b
           lock `.bilingual-interleaved`.
      215  

● Updated plan
  ⎿  /plan to preview

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 1 line, removed 1 line
      290  
      291  | Entity | Type | File:line | St | Role | Key signatures / fie
           lds |
      292  |---|---|---|---|---|---|
      293 -| `ThemeFamily` / `ThemeMode` | types | `src/core/theme/themes
          -.ts:1` | P-E | `'glass-acrylic'\|'glass-clear'\|'skeuomorphic'
          -\|'retro-futurist'\|'brutalist'\|'neo-brutalist'`; `'light'\|'
          -dark'` | — |                                                  
      293 +| `ThemeFamily` / `ThemeMode` | types | `src/core/theme/themes
          +.ts:1` | P-E | `'glass-acrylic'\|'glass-clear'\|'skeuomorphic'
          +\|'retro-futurist'\|'brutalist'\|'neo-brutalist'\|'sanctissimi
          +ssa'` (§7.7); `'light'\|'dark'` | — |                         
      294  | `THEME_FAMILIES` / `DEFAULT_FAMILY` | consts | `src/core/the
           me/themes.ts` | P-E | picker metadata; default `'skeuomorphic'
           ` | `{ id: ThemeFamily; label: string }[]` |
      295  | `applyTheme` / `systemMode` | fns | `src/core/theme/themes.t
           s` | P-E | sets `data-theme` + `data-mode` on `<html>`; `prefe
           rs-color-scheme` probe | `applyTheme(family: ThemeFamily, mode
           : ThemeMode): void`, `systemMode(): ThemeMode` |
      296  | `ExportOpts` / `exportHtml` / `exportMarkdown` / `exportJson
           ` / `downloadFile` | type/fns | `src/core/export/exporters.ts:
           1` | P-F | serialize current day/hour entries ± annotations | 
           `exportHtml(day: DayInfo, entries: ReaderEntry[], opts: Export
           Opts): string` (same shape for Md/Json); `ExportOpts { include
           Annotations: boolean; annotations: Annotation[] }`; `downloadF
           ile(name, mime, content)` |

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 22 lines
      354  | `VENDORED/haydock/` | vendored corpus | `VENDORED/haydock/PR
           OVENANCE.md` | P-S | public-domain DR commentary, verse-keyed;
            read-only layer in BibleView; `vendored` material in StudyBui
           lder | vendoring regime: provenance lock before assimilation |
      355  | tests | node:test | `tests/{bible,accompaniment}.test.ts` | 
           P-S | ingest counts (73 books, canon verse counts, Gen 1:1 exa
           ct, CITES spots); selector resolution incl. moveable feasts ac
           ross year boundaries; migration | `npm test` |
      356  
      357 +### Presentation & meaning plane (v0.5, §7.7 — status P-T)    
      358 +                                                              
      359 +| Entity | Type | File:line | St | Role | Key signatures / fie
          +lds |                                                         
      360 +|---|---|---|---|---|---|                                     
      361 +| `dialogueClass` | fn | `src/core/text/dialogue.ts:1` | P-T |
          + line-prefix → liturgical text-role class (render-level only; 
          +corpus text untouched) | `dialogueClass(line: string): 'dialog
          +ue-p' \| 'dialogue-s' \| null` (`V.`/`℣.`/`P.` → p; `R.`/`℟.`/
          +`S.` → s) |                                                   
      362 +| `--card` / `--card-shadow` / `--rubric` / `--dialogue-p` / `
          +--dialogue-s` | CSS tokens | `src/styles.css` | P-T | card-sur
          +face + text-role tokens, defined in every family block | — |  
      363 +| `sanctissimissa` theme blocks | CSS | `src/styles.css` | P-T
          + | `html[data-theme='sanctissimissa'][data-mode='light'\|'dark
          +']` token blocks per §7.7 | — |                               
      364 +| `BilingualText` | comp | `src/ui/BilingualText.tsx:1` | P-T 
          +| shared bilingual renderer (extracted from ReaderView TextBlo
          +ck): columns \| interleaved; text-role classes; echo/quote mar
          +ks | props `{ latin: string \| null; english: string \| null; 
          +quotes?: string[]; echoLine?: number; layout: 'columns' \| 'in
          +terleaved' }` |                                               
      365 +| `useNarrow` | hook | `src/ui/BilingualText.tsx` | P-T | matc
          +hMedia width probe driving the interleave switch | `useNarrow(
          +px?: number): boolean` (default 1100) |                       
      366 +| `.bilingual-interleaved` | CSS | `src/styles.css` | P-T | in
          +terleaved pair styling: `.il-la` bold `--ink`; `.il-en` indent
          + 1.1em italic `--ink-faint`; pair gap | — |                   
      367 +| `Clause` / `splitClauses` / `bestClause` | type/fns | `src/c
          +ore/vector/clause.ts:1` | P-T | clause segmentation + best-cla
          +use-vs-query via `embedText` cosine | `Clause { text; start; e
          +nd }`, `splitClauses(text: string): Clause[]`, `bestClause(tex
          +t: string, query: string): (Clause & { score: number }) \| nul
          +l` |                                                          
      368 +| `SimilarityGlyph` | comp | `src/ui/SimilarityGlyph.tsx:1` | 
          +P-T | icon-sized radial SVG: query center, hit dot radius ∝ (1
          +−score), siblings ghosted; score → tooltip | props `{ score: n
          +umber; siblings: number[]; size?: number }` |                 
      369 +| `IMAGERY_CONCEPTS` | const | `src/core/ontology/concepts.ts`
          + | P-T | ~15 imagery/metaphor/typology concepts (§7.7 list) me
          +rged into the exported taxonomy | same shape as existing `CONC
          +EPTS` entries |                                               
      370 +| `Pericope` / `PERICOPES` / `SCENARIO_CLUSTERS` | type/consts
          + | `src/core/ontology/parallels.ts:1` | P-T | curated Gospel-p
          +arallel spine + scenario clusters | `Pericope { id: string; ti
          +tle: string; cluster: string; refs: { Mt?: string; Mc?: string
          +; Lc?: string; Jo?: string } }` |                             
      371 +| `AtlasMode` / `ScriptureAtlas` | type/comp | `src/ui/Scriptu
          +reAtlas.tsx:1` | P-T | imagery + parallels navigation modes ho
          +sted by BibleView's mode switch | `AtlasMode = 'canonical' \| 
          +'imagery' \| 'parallels'`; props `{ db: CorpusDb; mode: AtlasM
          +ode; onOpenKey: (k: string) => void }` |                      
      372 +| `CorpusDb.commentaryFor` / `conceptVerseCounts` / `chapterCi
          +teCounts` | methods | `src/core/data/corpusDb.ts` | P-T | inte
          +rpretive-layer + atlas query surface | `commentaryFor(book: st
          +ring, ch: number, verse?: number): SectionText[]`, `conceptVer
          +seCounts(): { conceptId: string; label: string; count: number 
          +}[]`, `chapterCiteCounts(book: string): Map<number, number>` |
      373 +| `ingest-commentary` / `COMMENTARY_SOURCES` | script/const | 
          +`scripts/ingest-commentary.mjs:1` | P-T | `VENDORED/<source>/`
          + → `commentary:` nodes + `COMMENTS_ON` edges + embeddings + FT
          +S (§7.7) | invoked from `ingest-corpus.mjs`; `COMMENTARY_SOURC
          +ES: { id, dir, label, parse }[]` |                            
      374 +| `VENDORED/catena-aurea/` | vendored corpus | `VENDORED/caten
          +a-aurea/PROVENANCE.md` | P-T | Catena Aurea (Newman tr., PD), 
          +verse-keyed patristic chains; parallels cross-check source | v
          +endoring regime: provenance lock before assimilation |        
      375 +| `JournalSidecar` | comp | `src/ui/JournalSidecar.tsx:1` | P-
          +T | capture workspace pane (§7.7): source block, `Accompanimen
          +tEditor` embed, connections, destinations, toast | props `{ db
          +: CorpusDb; sidecar: SidecarDb; capture: { quote: string; quot
          +eAlt?: string; anchor: string \| null }; day: DayInfo \| null;
          + onClose: () => void; onOpenKey: (k: string) => void }` |     
      376 +| `ConnectionsPanel` | comp | `src/ui/JournalSidecar.tsx` | P-
          +T | why-bridge + evidence-chip connection cards over corpus ve
          +ctors + own accompaniments + commentary | props `{ db; sidecar
          +; text: string; anchor: string \| null; onAddSource: (k: strin
          +g) => void; onOpenKey }` |                                    
      377 +| tests | node:test | `tests/{clause,parallels}.test.ts` | P-T
          + | clause argmax determinism; pericope table integrity (refs p
          +arse, books exist) | `npm test` |                             
      378 +                                                              
      379  ## 9. Open questions resolved
      380  
      381  1. **Where does Office text assembly live?** In the data layer
            (`officeTexts.ts`), not in `OfficeView` — keeps the collinear
            rule (decision 1) and testability (`tests/officeTexts.test.ts
           ` runs headless).

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 1 line, removed 1 line
      383  3. **Sidecar persistence on web?** Single-blob IndexedDB write
            of the exported sql.js image, debounced — simplest crash-safe
            option without adding a dependency; OPFS rejected (Safari fri
           ction), localStorage rejected (size).
      384  4. **Homily yearly recycling model?** Base row (`year IS NULL`
           ) + per-year overlay rows sharing `liturgical_key` (decision 1
           1), not copy-on-write duplicates.
      385  5. **Do lore callouts gate on entitlements?** No — lore is cor
           e content; `FEATURE_GATES` covers planner/journal/themes-premi
           um/export/share/office and ships all-`null` anyway (G3).
      386 -6. **Theme count.** Plan says "6 families × light/dark" and al
          -so "twelve themes" — resolved as 6 × 2 = 12 (family list in `T
          -hemeFamily`).                                                 
      386 +6. **Theme count.** Plan says "6 families × light/dark" and al
          +so "twelve themes" — resolved as 6 × 2 = 12 (family list in `T
          +hemeFamily`). *Amended 2026-07-14 (§7.7):* + `sanctissimissa` 
          +⇒ 7 × 2 = 14; the v0.5 wave ships token blocks for `skeuomorph
          +ic` + `sanctissimissa` only, the other five families remain E2
          + work over the same token contract.                           
      387  7. **SUPERSEDED (operator, 2026-07-06) — full office engine is
            IN SCOPE.** The original v0.2 carve-out ("pattern-based assem
           bly; engine is backlog") was rejected: generating the complete
            Divine Office **is the product**. §7.5 is the binding contrac
           t; `HOUR_SECTION_PATTERNS` assembly survives only as the engin
           e's proper-section selection layer, never as the shipped depth
           . The "no invented content" rationale was a category error — t
           he engine deterministically *reckons* offices; implementing th
           at reckoning (schema + population + directive interpretation) 
           is the spec.
      388  8. **Shared-file ownership under parallel dispatch.** Exactly 
           one CHECKLIST task owns each shared shell file (`src/App.tsx` 
           → APP.1; `src/styles.css` → E2; `src/ui/SectionReader.tsx` cre
           ated by B3.1 with APP.2 as its same-worktree follow-up) so the
            whole wave dispatches concurrently without merge collisions (
           I-22 axis 1).
      389  

❯ Note that revenue cat configuration has begiun in devin ide; if you are not   
  authenticated in mcp to it yet, configure the mcp to auth by oauth2 if        
  possible or retrieve key from Admin-Manual and ensure configuration is        
  compatible with your gating pof entitlements regime                           

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 2 lines, removed 1 line
      385  5. **Do lore callouts gate on entitlements?** No — lore is cor
           e content; `FEATURE_GATES` covers planner/journal/themes-premi
           um/export/share/office and ships all-`null` anyway (G3).
      386  6. **Theme count.** Plan says "6 families × light/dark" and al
           so "twelve themes" — resolved as 6 × 2 = 12 (family list in `T
           hemeFamily`). *Amended 2026-07-14 (§7.7):* + `sanctissimissa` 
           ⇒ 7 × 2 = 14; the v0.5 wave ships token blocks for `skeuomorph
           ic` + `sanctissimissa` only, the other five families remain E2
            work over the same token contract.
      387  7. **SUPERSEDED (operator, 2026-07-06) — full office engine is
            IN SCOPE.** The original v0.2 carve-out ("pattern-based assem
           bly; engine is backlog") was rejected: generating the complete
            Divine Office **is the product**. §7.5 is the binding contrac
           t; `HOUR_SECTION_PATTERNS` assembly survives only as the engin
           e's proper-section selection layer, never as the shipped depth
           . The "no invented content" rationale was a category error — t
           he engine deterministically *reckons* offices; implementing th
           at reckoning (schema + population + directive interpretation) 
           is the spec.
      388 -8. **Shared-file ownership under parallel dispatch.** Exactly 
          -one CHECKLIST task owns each shared shell file (`src/App.tsx` 
          -→ APP.1; `src/styles.css` → E2; `src/ui/SectionReader.tsx` cre
          -ated by B3.1 with APP.2 as its same-worktree follow-up) so the
          - whole wave dispatches concurrently without merge collisions (
          -I-22 axis 1).                                                 
      388 +8. **Shared-file ownership under parallel dispatch.** Exactly 
          +one CHECKLIST task owns each shared shell file (`src/App.tsx` 
          +→ APP.1; `src/styles.css` → E2; `src/ui/SectionReader.tsx` cre
          +ated by B3.1 with APP.2 as its same-worktree follow-up) so the
          + whole wave dispatches concurrently without merge collisions (
          +I-22 axis 1). *Amended 2026-07-14 for the v0.5 wave:* `src/sty
          +les.css` → **BJ.2** (token refactor + `sanctissimissa` blocks 
          ++ every §7.7 CSS block — interleave, glyph, atlas, workspace);
          + `src/App.tsx` → **BO.3** (integration: ThemePicker mount, `Vi
          +ew` additions, `#/acc/` route); `src/ui/ReaderView.tsx` → **BK
          +.1** (BilingualText extraction) with BO.1 (ctx-menu capture) a
          +s its same-worktree follow-up; `src/ui/BibleView.tsx` → **BN.1
          +** (atlas modes + commentary layer) with BO.1's Bible half as 
          +follow-up.                                                    
      389 +9. **Gospel-parallels data source (operator, 2026-07-14).** Ve
          +ndor the public-domain interpretive sources of `DOCS/Scriptura
          +lReferences-PublicDomain.md` (this wave: Haydock + Catena Aure
          +a) rather than embedding-only derivation or a third-party syno
          +psis dataset; the curated `PERICOPES` spine is cross-checked a
          +gainst the vendored Catena during ingest, embeddings supply co
          +mplementarities only.                                         
      390  
      391  ## 10. Out of scope (v0.2)
      392  

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 4 lines, removed 2 lines
      404  
      405  **Attestation (2026-07-12, third re-attestation).** Amended pe
           r operator direction (this session): §7.6 Bible + Accompanimen
           t plane added (v0.4, labelled P-S in the entity table) — vendo
           red Bibles promoted to first-class graph citizens (book/chapte
           r/verse nodes, CITES edges, fill-normalization boundary), the 
           one-object/four-exposures Accompaniment unification (supersedi
           ng §7's `homilies`/`journal_entries`/`theme_spans` split and a
           bsorbing P-D `PlannerView`/`HomilyEditor`), sidecar-as-SQLite 
           v2 with lore + vector memory, journey-companion CompanionEngin
           e with SOUL.md-style CompanionMemory, entitlement vocabulary (
           `companion_ondevice`/`companion_hosted`/`institutional`), free
           -form theme selectors, ParishProfile header space, Haydock com
           mentary vendoring, deep-link address layer, Android widget.
      406  
      407 -**Attestation.** This document is complete, stub-free, and dep
          -icts the end-state production release: every named entity carr
          -ies an exact identifier, target `file:line`, role, and signatu
          -re; no TBD markers remain; open questions are resolved above. 
          -Shipped rows were verified against the working tree via codegr
          -aph on the date below; planned rows are normative targets and 
          -are cited verbatim by `CHECKLIST.md` stanzas (v0.2 wave, O-sta
          -nzas, B-stanzas). Re-attested after adding §7.6 + the P-S enti
          -ty rows and reconciling supersessions (PlannerView/HomilyEdito
          -r absorption, sidecar v2 schema, FeatureId/shareLink extension
          -s).                                                           
      408 -— Authored and attested by Claude Fable 5 (`claude-fable-5`, C
          -laude Code session, operator-directed architect pass) · 2026-0
          -7-12                                                          
      407 +**Attestation (2026-07-14, fourth re-attestation).** Amended p
          +er operator direction (this session): §7.7 presentation & mean
          +ing plane added (v0.5, labelled P-T in the entity table) — `sa
          +nctissimissa` theme family (7th family; decision 13 + open que
          +stion 6 amended; text-role tokens `--rubric`/`--dialogue-p`/`-
          +-dialogue-s` with render-level `dialogueClass`), interleaved b
          +ilingual mode (`BilingualText` extraction, selection-range ech
          +o), similarity UX (clause focus `bestClause`, `SimilarityGlyph
          +`, `IMAGERY_CONCEPTS`), Scripture Atlas (imagery/scenario + Go
          +spel-parallels navigation, `PERICOPES` spine), generalized int
          +erpretive layer (`ingest-commentary.mjs`, `COMMENTS_ON` edges;
          + Haydock + Catena Aurea this wave, 13-source PD roadmap), and 
          +the journal sidecar workspace (`JournalSidecar`/`ConnectionsPa
          +nel`, capture + highlight-both-panes context actions, destinat
          +ions → exposure/selectors). Open question 8 amended with v0.5 
          +shared-file ownership; open question 9 added (parallels data s
          +ource).                                                       
      408 +                                                              
      409 +**Attestation.** This document is complete, stub-free, and dep
          +icts the end-state production release: every named entity carr
          +ies an exact identifier, target `file:line`, role, and signatu
          +re; no TBD markers remain; open questions are resolved above. 
          +Shipped rows were verified against the working tree via codegr
          +aph on the date below; planned rows are normative targets and 
          +are cited verbatim by `CHECKLIST.md` stanzas (v0.2 wave, O-sta
          +nzas, B-stanzas, and the v0.5 BJ–BO stanzas). Re-attested afte
          +r adding §7.7 + the P-T entity rows.                          
      410 +— Authored and attested by Claude Fable 5 (`claude-fable-5`, C
          +laude Code session, operator-directed architect pass) · 2026-0
          +7-14                                                          

  Read 1 file (ctrl+o to expand)

● Update(CHECKLIST.md)
  ⎿  Added 34 lines
      502  - [ ] **BI.2** `CompanionMemory`: lore table + distillation lo
           op (idle/save; size-capped; user-visible/editable) + vector re
           call over `sidecar_embeddings` (embedText) fused with theme/da
           te facets.
      503  - [ ] **BI.3** `CompanionView` rail chat: context = persona+lo
           re+memories+position+CITES; replies cite deep links; save-insi
           ght → accompaniment(`generated`).
      504  - [ ] **BI.4** RC config against contract vocabulary (`compani
           on_ondevice`, `companion_hosted`, `institutional`) via RC plug
           in/MCP or dashboard (key per I-15); `FeatureId` gates wired. O
           n-device model run = TEST_RUBRIC operator row.
      505 +                                                              
      506 +---                                                           
      507 +                                                              
      508 +# v0.5 wave — Presentation & meaning plane (ARCHITECTURE §7.7,
          + entity rows P-T)                                             
      509 +                                                              
      510 +**Operator decisions recorded 2026-07-14:** (1) `sanctissimiss
          +a` is a **selectable alternate** family — `DEFAULT_FAMILY` sta
          +ys `skeuomorphic`; background colouring retained from the fram
          +ework; (2) mobile bilingual = interleaved Latin-bold / English
          +-inset rows with dual-language selection highlight — stacked f
          +ull columns are a defect; (3) similarity results must be claus
          +e-atomic with a relative-distance glyph and imagery/metaphor g
          +rouping; (4) Scripture navigation goes meaning-first (imagery/
          +scenarios, Gospel parallels, differential sizing) additively o
          +ver canonical; (5) interpretive data = **vendored public-domai
          +n sources** per `DOCS/ScripturalReferences-PublicDomain.md` (t
          +his wave Haydock + Catena Aurea; 13-source roadmap over the sa
          +me schema); (6) Journal/HMS per the sidecar prototype + PRD. S
          +hared-file owners for this wave: `styles.css` → BJ.2 · `App.ts
          +x` → BO.3 · `ReaderView.tsx` → BK.1 (BO.1 follow-up) · `BibleV
          +iew.tsx` → BN.1 (BO.1 follow-up).                             
      511 +                                                              
      512 +## Stanza B-J — Theme system + sanctissimissa family (E1 found
          +ation + §7.7)                                                 
      513 +- [ ] **BJ.1** `src/core/theme/themes.ts` per E1 spec with `Th
          +emeFamily` incl. `'sanctissimissa'`; `THEME_FAMILIES` (7 entri
          +es), `DEFAULT_FAMILY='skeuomorphic'`, `applyTheme`, `systemMod
          +e`. + `src/ui/ThemePicker.tsx` (family × mode per entity row; 
          +persists `theme.family`/`theme.mode` to sidecar settings, loca
          +lStorage fallback `sam.theme.v1` until BC.1 merges).          
      514 +- [ ] **BJ.2** `src/styles.css` (single owner this wave): refa
          +ctor onto semantic tokens (`--surface --surface-2 --ink --ink-
          +soft --accent --pane-latin-bg --pane-english-bg --rail-bg --ca
          +rd-border` + §7.7 `--card --card-shadow --rubric --dialogue-p 
          +--dialogue-s`); default `skeuomorphic` light block = current v
          +alues byte-for-byte visual parity; `sanctissimissa` light+dark
          + blocks (white elevated cards, accordion heads, propers gold-l
          +eft-border + warm tint); dark `skeuomorphic` block; **plus all
          + §7.7 component CSS**: `.bilingual-interleaved` (`.il-la` bold
          + / `.il-en` indent-italic-faint / pair gap), `.sim-glyph`, `.a
          +tlas-*` (label field, pericope rows), `.jsidecar-*` (workspace
          +, source block, connections cards, evidence chips, destination
          +s, toast).                                                    
      515 +- [ ] **BJ.3** `src/core/text/dialogue.ts`: `dialogueClass(lin
          +e)` per entity row; consumed by `BilingualText` to wrap prefix
          + tokens in `.dialogue-p`/`.dialogue-s` spans (render-only; cor
          +pus text unmodified).                                         
      516 +                                                              
      517 +## Stanza B-K — Interleaved bilingual mode                    
      518 +- [ ] **BK.1** Extract `src/ui/BilingualText.tsx` (`BilingualT
          +ext` + `useNarrow`) from ReaderView's TextBlock; columns mode 
          += behavior parity (echo, quotes `mark.ann`, `data-line`); Read
          +erView adopts it (ReaderView owner task this wave).           
      519 +- [ ] **BK.2** Interleaved mode: `layout='interleaved'` render
          +s Latin/English line pairs per §7.7; `useNarrow(1100)` drives 
          +the switch in ReaderView + BibleView (verse-pair granularity);
          + `selectionchange` echo extended to the selection's full line-
          +range in both directions.                                     
      520 +                                                              
      521 +## Stanza B-L — Similarity UX                                 
      522 +- [ ] **BL.1** `src/core/vector/clause.ts`: `Clause`, `splitCl
          +auses`, `bestClause` per entity row + `tests/clause.test.ts` (
          +determinism, min-length, argmax).                             
      523 +- [ ] **BL.2** `src/ui/SimilarityGlyph.tsx` + MeaningPanel int
          +egration: every similar-hit renders glyph + best-clause emphas
          +is (rest behind "more"); score → tooltip.                     
      524 +- [ ] **BL.3** `IMAGERY_CONCEPTS` (~15, §7.7 list) merged into
          + `src/core/ontology/concepts.ts`; shared re-ingest with BM.3 (
          +one `assets/missal.db` regeneration; fill-log delta must be ze
          +ro).                                                          
      525 +                                                              
      526 +## Stanza B-M — Interpretive layer (PD vendoring + commentary 
          +ingest)                                                       
      527 +- [ ] **BM.1** Vendor `VENDORED/haydock/` (fulfils BF.1): clon
          +e-at-home, PROVENANCE.md (source URL + commit/edition + date +
          + license note) BEFORE assimilation; additive only.            
      528 +- [ ] **BM.2** Vendor `VENDORED/catena-aurea/` (Newman tr., PD
          +): same regime.                                               
      529 +- [ ] **BM.3** `scripts/ingest-commentary.mjs` (`COMMENTARY_SO
          +URCES`, wired into `ingest-corpus.mjs`): `commentary:<source>/
          +<Book>/<ch>/<v>` nodes, English text_blocks, `COMMENTS_ON` edg
          +es, embeddings + FTS; `CorpusDb.commentaryFor` + adapter mirro
          +r; run shared re-ingest (with BL.3); fill-log delta zero; LFS 
          +push.                                                         
      530 +- [ ] **BM.4** `src/core/ontology/parallels.ts`: `Pericope`/`P
          +ERICOPES` (~60 spine, cross-checked against vendored Catena) +
          + `SCENARIO_CLUSTERS`; `tests/parallels.test.ts` (refs parse, b
          +ooks exist in BOOK_MAP).                                      
      531 +                                                              
      532 +## Stanza B-N — Scripture Atlas                               
      533 +- [ ] **BN.1** `src/ui/ScriptureAtlas.tsx` (`AtlasMode`, image
          +ry label-field + parallels aligned rows per §7.7) + BibleView 
          +mode switch + commentary read-only layer beneath verses (Bible
          +View owner task) + `CorpusDb.conceptVerseCounts`/`chapterCiteC
          +ounts`.                                                       
      534 +                                                              
      535 +## Stanza B-O — Journal sidecar workspace (after B-C core)    
      536 +- [ ] **BO.1** Capture + highlight context actions: ReaderView
          +/BibleView ctx-menus gain "✎ Add to Journal/Homily notes" (ope
          +ns JournalSidecar with quote + `alignSelection` counterpart + 
          +anchor) and "🖍 Highlight both panes" (lightweight accompanimen
          +t, quote+quoteAlt through `mark.ann` in both panes).          
      537 +- [ ] **BO.2** `src/ui/JournalSidecar.tsx` (`JournalSidecar` +
          + `ConnectionsPanel` per entity rows): source block, `Accompani
          +mentEditor` embed, connections cards (corpus vectors + own acc
          +ompaniments via `sidecar_embeddings` + commentary; why-bridge 
          ++ evidence chips + add/dismiss), destinations → exposure/selec
          +tors, toast.                                                  
      538 +- [ ] **BO.3** Integration (App owner): `View` gains `'journal
          +'`; NAV "Journal & Homilies" (✎); `#/acc/<id>` route resolves 
          +(completes BB.3); ThemePicker mounted in rail; JournalView/Hom
          +ilyPlanner (BD.1/BD.2) reachable from the journal view's tabs.
  Committed 086f8e7f, pushed to master, read 1 file, ran 1 shell command (ctrl+o to expand)

● No RevenueCat MCP is configured yet. Let me find the RC credentials in
  Admin-Manual:

  Searched for 1 pattern, read 1 file, ran 4 shell commands (ctrl+o to expand)

● MCP server registered and connected. Now creating the contract vocabulary
  (additive — nothing existing is touched):

  Ran 1 shell command (ctrl+o to expand)

● Update(~/Admin-Manual/DOCS/revenuecat/ROBINS-AI-WORLD-CATALOG.md)
  ⎿  Added 1 line
      12  | `appd691c14ef3` | prefetch (Web Billing) | rc_billing | |
      13  | `app91d5afe016` | Robin's AI World (Stripe) | stripe | |
      14  | `appe9a6cf9592` | General Cheung Chinese Chess (Web Billing) 
          | rc_billing | |
      15 +| `app8f455368d3` | St. Android's Missal | play_store | `mba.ro
         +bin.standroidsmissal` |                                        
      16  
      17  ## Entitlements
      18  

● Update(~/Admin-Manual/DOCS/revenuecat/ROBINS-AI-WORLD-CATALOG.md)
  ⎿  Added 3 lines
      22  | `hosted_ai` | Hosted AI path gate (optional) |
      23  | `realtime` | Prefetch realtime AI processing |
      24  | `save_artifact` | Prefetch save/export |
      25 +| `companion_ondevice` | St. Android's Missal — unlimited on-de
         +vice Companion (`entl915eda495b`) |                            
      26 +| `companion_hosted` | St. Android's Missal — metered hosted Co
         +mpanion (`entlddab18f5ab`) |                                   
      27 +| `institutional` | St. Android's Missal — parish edition: News
         +letterDesk + ParishProfile (`entla29f5bce2b`) |                
      28  
      29  ## Virtual currencies (project-level = shared across apps)
      30  

  Committed e469392, pushed to master (ctrl+o to expand)

● 4 background agents launched (↓ to manage)
   ├ W1: sidecar v2 foundation
   ├ W5: similarity UX overhaul
   ├ W6a: PD vendoring + commentary ingest
   └ W2: theme system + sanctissimissa

● Status update while Wave A runs — here's where things stand:

  Phase 1 — contract amendment: done and pushed (086f8e7f, both remotes).
  DOCS/ARCHITECTURE.md gained §7.7 (presentation & meaning plane, v0.5) with a
  full P-T entity table, decision 13 amended to seven theme families, open
  questions 6/8/9 updated, fourth re-attestation. CHECKLIST.md gained stanzas
  B-J through B-O with per-wave shared-file owners so the work fans out without
  merge collisions. Per your correction, the family is named sanctissimissa
  everywhere (the plan file and contract were scrubbed of the "sanctissima"
  typo).

  RevenueCat side quest: done. The Devin-side work had not landed in the shared
  project — projaba0bd94 had no St. Android's app and none of our entitlements.
  I've seeded it with the contract vocabulary so anything Devin does must
  reconcile to these:
  - MCP server registered at user scope (https://mcp.revenuecat.ai/mcp, status ✔
  Connected). It advertises OAuth, but that path requires an interactive
  browser login, so I authenticated with the canonical v2 secret key from
  Admin-Manual/CREDENTIALS/RevenueCat/credentials.env (key untouched, per I-15).
  Its tools become available next session start.
  - Created entitlements companion_ondevice (entl915eda495b), companion_hosted
  (entlddab18f5ab), institutional (entla29f5bce2b) and the Play Store app St. 
  Android's Missal (app8f455368d3, mba.robin.standroidsmissal).
  - ROBINS-AI-WORLD-CATALOG.md updated and pushed. Still open under BI.4:
  products/offerings attached to those entitlements, and the client-side public
  SDK key.

  Phase 2 Wave A: four agents running in parallel on disjoint files:
  1. W1 sidecar v2 — Accompaniment types/store/resolve, Tauri
  load_sidecar/save_sidecar, migration, tests (BC.1/2/4)
  2. W5 similarity UX — bestClause clause focus, SimilarityGlyph radial
  mini-map, 15 imagery concepts, MeaningPanel integration (BL.1–3)
  3. W6a interpretive layer — vendoring Haydock + Catena Aurea with provenance
  locks, ingest-commentary.mjs, CorpusDb.commentaryFor, the ~60-pericope
  parallels spine (BM.1–4)
  4. W2 theme — themes.ts, semantic-token refactor of styles.css (zero visual
  change to the default), sanctissimissa light+dark card blocks, dialogue/rubric
  tokens, ThemePicker (BJ.1–3)

  When they hand back I'll semantically verify each against its entity rows,
  commit per task, then dispatch Wave B (mobile interleave + the shared
  re-ingest) and Wave C (journal/HMS workspace + App integration).

✻ Sautéed for 19m 9s

● Agent "W5: similarity UX overhaul" finished · 5m 31s

● Wave A first hand-back (W5 similarity UX). Verifying semantically before
  committing — spot-checking the actual artifacts against the entity rows:

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(CHECKLIST.md)
  ⎿  Added 3 lines, removed 3 lines
      519  - [ ] **BK.2** Interleaved mode: `layout='interleaved'` render
           s Latin/English line pairs per §7.7; `useNarrow(1100)` drives 
           the switch in ReaderView + BibleView (verse-pair granularity);
            `selectionchange` echo extended to the selection's full line-
           range in both directions.
      520  
      521  ## Stanza B-L — Similarity UX
      522 -- [ ] **BL.1** `src/core/vector/clause.ts`: `Clause`, `splitCl
          -auses`, `bestClause` per entity row + `tests/clause.test.ts` (
          -determinism, min-length, argmax).                             
      523 -- [ ] **BL.2** `src/ui/SimilarityGlyph.tsx` + MeaningPanel int
          -egration: every similar-hit renders glyph + best-clause emphas
          -is (rest behind "more"); score → tooltip.                     
      524 -- [ ] **BL.3** `IMAGERY_CONCEPTS` (~15, §7.7 list) merged into
          - `src/core/ontology/concepts.ts`; shared re-ingest with BM.3 (
          -one `assets/missal.db` regeneration; fill-log delta must be ze
          -ro).                                                          
      522 +- [X] **BL.1** `src/core/vector/clause.ts`: `Clause`, `splitCl
          +auses`, `bestClause` per entity row + `tests/clause.test.ts` (
          +determinism, min-length, argmax). _2026-07-14: linear-scan spl
          +itter, exact offsets, 7 tests; suite 62/62._                  
      523 +- [X] **BL.2** `src/ui/SimilarityGlyph.tsx` + MeaningPanel int
          +egration: every similar-hit renders glyph + best-clause emphas
          +is (rest behind "more"); score → tooltip. _2026-07-14: golden-
          +angle deterministic layout; both grouped-similar call sites + 
          +ConceptTag thread query/siblings._                            
      524 +- [/] **BL.3** `IMAGERY_CONCEPTS` (~15, §7.7 list) merged into
          + `src/core/ontology/concepts.ts`; shared re-ingest with BM.3 (
          +one `assets/missal.db` regeneration; fill-log delta must be ze
          +ro). _2026-07-14: 15 concepts + `sacred_imagery` parent append
          +ed, flow into CONCEPTS verified (46 ids); re-ingest pending Wa
          +ve B._                                                        
      525  
      526  ## Stanza B-M — Interpretive layer (PD vendoring + commentary 
           ingest)
      527  - [ ] **BM.1** Vendor `VENDORED/haydock/` (fulfils BF.1): clon
           e-at-home, PROVENANCE.md (source URL + commit/edition + date +
            license note) BEFORE assimilation; additive only.

● Agent "W1: sidecar v2 foundation" finished · 6m 24s
● W1 handed back clean (cargo check green, 73 tests). Spot-checking the
  artifacts:

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(CHECKLIST.md)
  ⎿  Added 3 lines, removed 3 lines
      474  - [/] **BB.3** Deep-link routes on `shareLink.ts` (P-F extensi
           on): `#/verse/…`, `#/acc/…`, `#/day/…`, `#/section/…` wired in
           to App's layered back-nav. _2026-07-12: verse/day/section live
            (#/verse/Ps/22/2 browser-verified incl. verse highlight+scrol
           l); #/acc/ lands with the B-C store._
      475  
      476  ## Stanza B-C — Accompaniment model + sidecar v2 + editor
      477 -- [ ] **BC.1** `src/core/accompaniment/types.ts` (`Accompanime
          -nt`, `OccurrenceSelector`, `Exposure`) + `store.ts` (`SIDECAR_
          -SCHEMA_SQL_V2`, `SidecarDb` v2, platform byte-persistence web 
          -OPFS/IndexedDB + Tauri `load_sidecar`/`save_sidecar`); annotat
          -ions-localStorage migration (old key read-only).              
      478 -- [ ] **BC.2** `resolve.ts`: `accompanimentsForDay`/`forAnchor
          -`/`matchesSelector` over computus + recurrence rules.         
      477 +- [X] **BC.1** `src/core/accompaniment/types.ts` (`Accompanime
          +nt`, `OccurrenceSelector`, `Exposure`) + `store.ts` (`SIDECAR_
          +SCHEMA_SQL_V2`, `SidecarDb` v2, platform byte-persistence web 
          +OPFS/IndexedDB + Tauri `load_sidecar`/`save_sidecar`); annotat
          +ions-localStorage migration (old key read-only). _2026-07-14: 
          ++`quote_alt` col (§7.7 dual-pane highlight) w/ ensureColumn up
          +grade; migration reads both `sam.annotations.v1` and the store
          +'s real `standroidsmissal.annotations.v1`; Rust cmds cargo-che
          +ck clean._                                                    
      478 +- [X] **BC.2** `resolve.ts`: `accompanimentsForDay`/`forAnchor
          +`/`matchesSelector` over computus + recurrence rules. _2026-07
          +-14: recurrence grammar `weekly:<0-6>` / `nth-weekday:<n>:<0-6
          +>`._                                                          
      479  - [ ] **BC.3** `AccompanimentEditor.tsx` (TipTap; body_pm + bo
           dy_html snapshot).
      480 -- [ ] **BC.4** `tests/accompaniment.test.ts`: selector resolut
          -ion incl. moveable feasts across year boundaries; migration; f
          -ree-form theme tags.                                          
      480 +- [X] **BC.4** `tests/accompaniment.test.ts`: selector resolut
          +ion incl. moveable feasts across year boundaries; migration; f
          +ree-form theme tags. _2026-07-14: 10 tests incl. Pasc0-2 2026↔
          +2027 boundary + first-Friday recurrence; suite 73/73._        
      481  
      482  ## Stanza B-D — Exposure surfaces
      483  - [ ] **BD.1** `JournalView` (date timeline; day-chip today's 
           entries).

  Ran 1 shell command (ctrl+o to expand)

● Agent(W3: interleaved bilingual mode)
  ⎿  Backgrounded agent (↓ to manage · ctrl+o to expand)

● W3 (interleaved bilingual) dispatched. Current state: W1 sidecar and W5 
  similarity are verified and committed; three agents now running in parallel —
  W2 (theme system + sanctissimissa, owns styles.css), W6a (PD vendoring +
  commentary ingest + parallels spine), and W3 (interleave + selection-range
  echo, owns ReaderView/BibleView). Wave C (journal workspace + App integration)
  queues behind W3 since it edits the same reader files.

✻ Churned for 1m 59s

● Agent "W2: theme system + sanctissimissa" finished · 6m 44s

● W2 (theme) handed back with clean build. Spot-checking the token refactor and
  theme blocks:

  Ran 1 shell command (ctrl+o to expand)

● W2 verifies cleanly. Flipping B-J markers and committing:

● Update(CHECKLIST.md)
  ⎿  Added 3 lines, removed 3 lines
      510  **Operator decisions recorded 2026-07-14:** (1) `sanctissimiss
           a` is a **selectable alternate** family — `DEFAULT_FAMILY` sta
           ys `skeuomorphic`; background colouring retained from the fram
           ework; (2) mobile bilingual = interleaved Latin-bold / English
           -inset rows with dual-language selection highlight — stacked f
           ull columns are a defect; (3) similarity results must be claus
           e-atomic with a relative-distance glyph and imagery/metaphor g
           rouping; (4) Scripture navigation goes meaning-first (imagery/
           scenarios, Gospel parallels, differential sizing) additively o
           ver canonical; (5) interpretive data = **vendored public-domai
           n sources** per `DOCS/ScripturalReferences-PublicDomain.md` (t
           his wave Haydock + Catena Aurea; 13-source roadmap over the sa
           me schema); (6) Journal/HMS per the sidecar prototype + PRD. S
           hared-file owners for this wave: `styles.css` → BJ.2 · `App.ts
           x` → BO.3 · `ReaderView.tsx` → BK.1 (BO.1 follow-up) · `BibleV
           iew.tsx` → BN.1 (BO.1 follow-up).
      511  
      512  ## Stanza B-J — Theme system + sanctissimissa family (E1 found
           ation + §7.7)
      513 -- [ ] **BJ.1** `src/core/theme/themes.ts` per E1 spec with `Th
          -emeFamily` incl. `'sanctissimissa'`; `THEME_FAMILIES` (7 entri
          -es), `DEFAULT_FAMILY='skeuomorphic'`, `applyTheme`, `systemMod
          -e`. + `src/ui/ThemePicker.tsx` (family × mode per entity row; 
          -persists `theme.family`/`theme.mode` to sidecar settings, loca
          -lStorage fallback `sam.theme.v1` until BC.1 merges).          
      514 -- [ ] **BJ.2** `src/styles.css` (single owner this wave): refa
          -ctor onto semantic tokens (`--surface --surface-2 --ink --ink-
          -soft --accent --pane-latin-bg --pane-english-bg --rail-bg --ca
          -rd-border` + §7.7 `--card --card-shadow --rubric --dialogue-p 
          ---dialogue-s`); default `skeuomorphic` light block = current v
          -alues byte-for-byte visual parity; `sanctissimissa` light+dark
          - blocks (white elevated cards, accordion heads, propers gold-l
          -eft-border + warm tint); dark `skeuomorphic` block; **plus all
          - §7.7 component CSS**: `.bilingual-interleaved` (`.il-la` bold
          - / `.il-en` indent-italic-faint / pair gap), `.sim-glyph`, `.a
          -tlas-*` (label field, pericope rows), `.jsidecar-*` (workspace
          -, source block, connections cards, evidence chips, destination
          -s, toast).                                                    
      515 -- [ ] **BJ.3** `src/core/text/dialogue.ts`: `dialogueClass(lin
          -e)` per entity row; consumed by `BilingualText` to wrap prefix
          - tokens in `.dialogue-p`/`.dialogue-s` spans (render-only; cor
          -pus text unmodified).                                         
      513 +- [X] **BJ.1** `src/core/theme/themes.ts` per E1 spec with `Th
          +emeFamily` incl. `'sanctissimissa'`; `THEME_FAMILIES` (7 entri
          +es), `DEFAULT_FAMILY='skeuomorphic'`, `applyTheme`, `systemMod
          +e`. + `src/ui/ThemePicker.tsx` (family × mode per entity row; 
          +persists `theme.family`/`theme.mode` to sidecar settings, loca
          +lStorage fallback `sam.theme.v1` until BC.1 merges). _2026-07-
          +14: 3-state mode toggle (light/dark/system, system resolves vi
          +a systemMode before applyTheme); mounted in App by BO.3._     
      514 +- [X] **BJ.2** `src/styles.css` (single owner this wave): refa
          +ctor onto semantic tokens (`--surface --surface-2 --ink --ink-
          +soft --accent --pane-latin-bg --pane-english-bg --rail-bg --ca
          +rd-border` + §7.7 `--card --card-shadow --rubric --dialogue-p 
          +--dialogue-s`); default `skeuomorphic` light block = current v
          +alues byte-for-byte visual parity; `sanctissimissa` light+dark
          + blocks (white elevated cards, accordion heads, propers gold-l
          +eft-border + warm tint); dark `skeuomorphic` block; **plus all
          + §7.7 component CSS**: `.bilingual-interleaved` (`.il-la` bold
          + / `.il-en` indent-italic-faint / pair gap), `.sim-glyph`, `.a
          +tlas-*` (label field, pericope rows), `.jsidecar-*` (workspace
          +, source block, connections cards, evidence chips, destination
          +s, toast). _2026-07-14: 486→680 lines; token aliases keep skeu
          +omorphic-light computed values identical; dark block reuses th
          +e operator's prototype light↔dark palette; dangling BibleView 
          +--surface-2/--card-border now resolve; vite build ✓._         
      515 +- [X] **BJ.3** `src/core/text/dialogue.ts`: `dialogueClass(lin
          +e)` per entity row; consumed by `BilingualText` to wrap prefix
          + tokens in `.dialogue-p`/`.dialogue-s` spans (render-only; cor
          +pus text unmodified). _2026-07-14: module shipped; BilingualTe
          +xt consumption wires in with BK.1._                           
      516  
      517  ## Stanza B-K — Interleaved bilingual mode
      518  - [ ] **BK.1** Extract `src/ui/BilingualText.tsx` (`BilingualT
           ext` + `useNarrow`) from ReaderView's TextBlock; columns mode 
           = behavior parity (echo, quotes `mark.ann`, `data-line`); Read
           erView adopts it (ReaderView owner task this wave).

  Ran 1 shell command (ctrl+o to expand)

● W2 committed. Pushing the three workstream commits to both remotes while W6a
  (vendoring) and W3 (interleave) continue:

  Pushed to master (ctrl+o to expand)

● Three of five workstreams are now verified, committed, and pushed to both
  remotes (457c9277):

  - W1 sidecar v2 (BC.1/2/4 [X]) — schema + store + selector resolution, Tauri
  commands cargo-check clean, 10 new tests
  - W5 similarity UX (BL.1/2 [X], BL.3 [/]) — clause focus, radial glyph,
  imagery concepts awaiting the shared re-ingest
  - W2 theme (BJ.1–3 [X]) — token refactor with zero default-theme change,
  sanctissimissa light+dark, skeuomorphic dark, ThemePicker ready to mount

  Still running: W6a (Haydock + Catena Aurea vendoring, commentary ingest,
  parallels spine — the long pole, involves network research and downloads) and
  W3 (interleaved bilingual + selection-range echo). When those land: the shared
  re-ingest (BL.3 + BM.3 into one missal.db regeneration), then Wave C — the
  journal/HMS workspace (BO.1–3 + BD.1/2) and Scripture Atlas (BN.1).

✻ Baked for 58s

● Agent "W3: interleaved bilingual mode" finished · 4m 55s

● W3 handed back clean. Verifying the interleave renderer and echo-range
  mechanics:

  Ran 2 shell commands (ctrl+o to expand)

● Suite green. Committing W3 and dispatching Wave C1 (the journal workspace
  components — all new files, so it can run while W6a finishes):

● Update(CHECKLIST.md)
  ⎿  Added 2 lines, removed 2 lines
      515  - [X] **BJ.3** `src/core/text/dialogue.ts`: `dialogueClass(lin
           e)` per entity row; consumed by `BilingualText` to wrap prefix
            tokens in `.dialogue-p`/`.dialogue-s` spans (render-only; cor
           pus text unmodified). _2026-07-14: module shipped; BilingualTe
           xt consumption wires in with BK.1._
      516  
      517  ## Stanza B-K — Interleaved bilingual mode
      518 -- [ ] **BK.1** Extract `src/ui/BilingualText.tsx` (`BilingualT
          -ext` + `useNarrow`) from ReaderView's TextBlock; columns mode 
          -= behavior parity (echo, quotes `mark.ann`, `data-line`); Read
          -erView adopts it (ReaderView owner task this wave).           
      519 -- [ ] **BK.2** Interleaved mode: `layout='interleaved'` render
          -s Latin/English line pairs per §7.7; `useNarrow(1100)` drives 
          -the switch in ReaderView + BibleView (verse-pair granularity);
          - `selectionchange` echo extended to the selection's full line-
          -range in both directions.                                     
      518 +- [X] **BK.1** Extract `src/ui/BilingualText.tsx` (`BilingualT
          +ext` + `useNarrow`) from ReaderView's TextBlock; columns mode 
          += behavior parity (echo, quotes `mark.ann`, `data-line`); Read
          +erView adopts it (ReaderView owner task this wave). _2026-07-1
          +4: TextLines = old TextBlock verbatim; dialogueClass wired in 
          +both layouts (BJ.3 consumption)._                             
      519 +- [X] **BK.2** Interleaved mode: `layout='interleaved'` render
          +s Latin/English line pairs per §7.7; `useNarrow(1100)` drives 
          +the switch in ReaderView + BibleView (verse-pair granularity);
          + `selectionchange` echo extended to the selection's full line-
          +range in both directions. _2026-07-14: echo state → {nodeKey, 
          +from, to}; anchor+focus both resolved; BibleView pairs carry b
          +ible-verse+il-pair so focus/deep-link/annotate flows unchanged
          +; suite fail 0, tsc clean._                                   
      520  
      521  ## Stanza B-L — Similarity UX
      522  - [X] **BL.1** `src/core/vector/clause.ts`: `Clause`, `splitCl
           auses`, `bestClause` per entity row + `tests/clause.test.ts` (
           determinism, min-length, argmax). _2026-07-14: linear-scan spl
           itter, exact offsets, 7 tests; suite 62/62._
  Pushed to master (ctrl+o to expand)

● Agent(C1: journal workspace components)
  ⎿  Backgrounded agent (↓ to manage · ctrl+o to expand)

● W3 committed and pushed (8265e90c). Now running in parallel: W6a (PD vendoring
  + commentary ingest — still the long pole) and C1 (journal workspace
  components: TipTap editor, JournalSidecar with connections panel, JournalView,
  HomilyPlanner — all new files, no conflicts).

  Remaining after those: C2 integration (App rail/routes/ctx-menus — needs C1),
  BN.1 Scripture Atlas (needs W6a's parallels + query methods), and the single
  shared re-ingest that lands the imagery concepts + commentary into missal.db.

✻ Churned for 1m 51s

● Agent "W6a: PD vendoring + commentary ingest" finished · 14m 0s

● W6a delivered — full 73-book Haydock plus the complete Newman Catena Aurea,
  both provenance-locked, and it surfaced a real pre-existing gap in the
  Douay-Rheims JSON (missing trailing chapters in several books). Verifying,
  then committing and starting the shared re-ingest:

  Searched for 1 pattern (ctrl+o to expand)

● Update(DOCS/ARCHITECTURE.md)
  ⎿  Added 1 line, removed 1 line
      367  | `Clause` / `splitClauses` / `bestClause` | type/fns | `src/c
           ore/vector/clause.ts:1` | P-T | clause segmentation + best-cla
           use-vs-query via `embedText` cosine | `Clause { text; start; e
           nd }`, `splitClauses(text: string): Clause[]`, `bestClause(tex
           t: string, query: string): (Clause & { score: number }) \| nul
           l` |
      368  | `SimilarityGlyph` | comp | `src/ui/SimilarityGlyph.tsx:1` | 
           P-T | icon-sized radial SVG: query center, hit dot radius ∝ (1
           −score), siblings ghosted; score → tooltip | props `{ score: n
           umber; siblings: number[]; size?: number }` |
      369  | `IMAGERY_CONCEPTS` | const | `src/core/ontology/concepts.ts`
            | P-T | ~15 imagery/metaphor/typology concepts (§7.7 list) me
           rged into the exported taxonomy | same shape as existing `CONC
           EPTS` entries |
      370 -| `Pericope` / `PERICOPES` / `SCENARIO_CLUSTERS` | type/consts
          - | `src/core/ontology/parallels.ts:1` | P-T | curated Gospel-p
          -arallel spine + scenario clusters | `Pericope { id: string; ti
          -tle: string; cluster: string; refs: { Mt?: string; Mc?: string
          -; Lc?: string; Jo?: string } }` |                             
      370 +| `Pericope` / `PERICOPES` / `SCENARIO_CLUSTERS` | type/consts
          + | `src/core/ontology/parallels.ts:1` | P-T | curated Gospel-p
          +arallel spine + scenario clusters | `Pericope { id: string; ti
          +tle: string; cluster: string; refs: { Matt?: string; Marc?: st
          +ring; Luc?: string; Joann?: string } }` (keys = BOOK_MAP canon
          +ical Gospel keys) |                                           
      371  | `AtlasMode` / `ScriptureAtlas` | type/comp | `src/ui/Scriptu
           reAtlas.tsx:1` | P-T | imagery + parallels navigation modes ho
           sted by BibleView's mode switch | `AtlasMode = 'canonical' \| 
           'imagery' \| 'parallels'`; props `{ db: CorpusDb; mode: AtlasM
           ode; onOpenKey: (k: string) => void }` |
      372  | `CorpusDb.commentaryFor` / `conceptVerseCounts` / `chapterCi
           teCounts` | methods | `src/core/data/corpusDb.ts` | P-T | inte
           rpretive-layer + atlas query surface | `commentaryFor(book: st
           ring, ch: number, verse?: number): SectionText[]`, `conceptVer
           seCounts(): { conceptId: string; label: string; count: number 
           }[]`, `chapterCiteCounts(book: string): Map<number, number>` |
      373  | `ingest-commentary` / `COMMENTARY_SOURCES` | script/const | 
           `scripts/ingest-commentary.mjs:1` | P-T | `VENDORED/<source>/`
            → `commentary:` nodes + `COMMENTS_ON` edges + embeddings + FT
           S (§7.7) | invoked from `ingest-corpus.mjs`; `COMMENTARY_SOURC
           ES: { id, dir, label, parse }[]` |

● Update(CHECKLIST.md)
  ⎿  Added 4 lines, removed 4 lines
      524  - [/] **BL.3** `IMAGERY_CONCEPTS` (~15, §7.7 list) merged into
            `src/core/ontology/concepts.ts`; shared re-ingest with BM.3 (
           one `assets/missal.db` regeneration; fill-log delta must be ze
           ro). _2026-07-14: 15 concepts + `sacred_imagery` parent append
           ed, flow into CONCEPTS verified (46 ids); re-ingest pending Wa
           ve B._
      525  
      526  ## Stanza B-M — Interpretive layer (PD vendoring + commentary 
           ingest)
      527 -- [ ] **BM.1** Vendor `VENDORED/haydock/` (fulfils BF.1): clon
          -e-at-home, PROVENANCE.md (source URL + commit/edition + date +
          - license note) BEFORE assimilation; additive only.            
      528 -- [ ] **BM.2** Vendor `VENDORED/catena-aurea/` (Newman tr., PD
          -): same regime.                                               
      529 -- [ ] **BM.3** `scripts/ingest-commentary.mjs` (`COMMENTARY_SO
          -URCES`, wired into `ingest-corpus.mjs`): `commentary:<source>/
          -<Book>/<ch>/<v>` nodes, English text_blocks, `COMMENTS_ON` edg
          -es, embeddings + FTS; `CorpusDb.commentaryFor` + adapter mirro
          -r; run shared re-ingest (with BL.3); fill-log delta zero; LFS 
          -push.                                                         
      530 -- [ ] **BM.4** `src/core/ontology/parallels.ts`: `Pericope`/`P
          -ERICOPES` (~60 spine, cross-checked against vendored Catena) +
          - `SCENARIO_CLUSTERS`; `tests/parallels.test.ts` (refs parse, b
          -ooks exist in BOOK_MAP).                                      
      527 +- [X] **BM.1** Vendor `VENDORED/haydock/` (fulfils BF.1): clon
          +e-at-home, PROVENANCE.md (source URL + commit/edition + date +
          + license note) BEFORE assimilation; additive only. _2026-07-14
          +: 1883 Dunigan expanded edition (USFM, complete 73-book canon,
          + 20,965 records) from cmahte/ENG-B-Haydock1883-pd-PSFM@0332c84
          +; .git removed post-lock._                                    
      528 +- [X] **BM.2** Vendor `VENDORED/catena-aurea/` (Newman tr., PD
          +): same regime. _2026-07-14: Newman/Parker 1841–45 via tj06man
          +/catena-aurea-project@d3829bb (wording verified); 816 pericope
          + blocks, 4 Gospels._                                          
      529 +- [/] **BM.3** `scripts/ingest-commentary.mjs` (`COMMENTARY_SO
          +URCES`, wired into `ingest-corpus.mjs`): `commentary:<source>/
          +<Book>/<ch>/<v>` nodes, English text_blocks, `COMMENTS_ON` edg
          +es, embeddings + FTS; `CorpusDb.commentaryFor` + adapter mirro
          +r; run shared re-ingest (with BL.3); fill-log delta zero; LFS 
          +push. _2026-07-14: script + query surface shipped, scratch run
          + 20,915 nodes / 24,244 edges; SHARED RE-INGEST PENDING. Findin
          +g: pre-existing DR JSON gap (Num 36, Jos 23–24, Judg 18–21, Ru
          +th 3–4, Judith 12–16, Esth 11+, Eccli 49–51, …) strands 865 Ha
          +ydock records as skips — complete the DR source in a BA follow
          +-up._                                                         
      530 +- [X] **BM.4** `src/core/ontology/parallels.ts`: `Pericope`/`P
          +ERICOPES` (~60 spine, cross-checked against vendored Catena) +
          + `SCENARIO_CLUSTERS`; `tests/parallels.test.ts` (refs parse, b
          +ooks exist in BOOK_MAP). _2026-07-14: 79 pericopes / 8 cluster
          +s, refs keyed Matt/Marc/Luc/Joann (BOOK_MAP canonical; entity 
          +row reconciled), 152 refs machine-verified vs vendored DR; sui
          +te 79/79._                                                    
      531  
      532  ## Stanza B-N — Scripture Atlas
      533  - [ ] **BN.1** `src/ui/ScriptureAtlas.tsx` (`AtlasMode`, image
           ry label-field + parallels aligned rows per §7.7) + BibleView 
           mode switch + commentary read-only layer beneath verses (Bible
           View owner task) + `CorpusDb.conceptVerseCounts`/`chapterCiteC
           ounts`.

  Ran 1 shell command (ctrl+o to expand)

● W6a committed. Launching the shared re-ingest in the background (imagery
  concepts + commentary → one missal.db regeneration):

  Ran 1 shell command (ctrl+o to expand)

● The re-ingest is running in the background (logging to .tmp/ingest-v05.log),
  and the C1 journal-workspace agent is still building. Two hand-backs remain
  before integration: the ingest result (fill-log delta must be zero, then LFS
  push of the regenerated db) and C1's components. After that comes the final
  integration pass — C2 (App rail/routes/context-menus) and BN.1 (Scripture
  Atlas, which now has everything it needs: PERICOPES, conceptVerseCounts,
  chapterCiteCounts, and commentary in the db).

✻ Brewed for 1m 9s

● Background command "Run shared re-ingest (imagery concepts + commentary)
logging to .tmp" completed (exit code 0)

  Searched for 2 patterns, ran 6 shell commands (ctrl+o to expand)

● Update(scripts/ingest-corpus.mjs)
  ⎿  Added 1 line, removed 1 line
      31  import { ingestBiblePlane } from './ingest-bible.mjs';
      32  import { ingestCommentary } from './ingest-commentary.mjs';
      33  import { Scripture } from './scripture.mjs';
      34 -import { CONCEPTS } from '../src/core/ontology/concepts.ts';   
      34 +import { CONCEPTS, IMAGERY_CONCEPTS } from '../src/core/ontolog
         +y/concepts.ts';                                                
      35  
      36  const WWW = resolve('VENDORED/divinum-officium/web/www');
      37  const OUT = process.argv[2] ?? 'assets/missal.db';

● Update(scripts/ingest-corpus.mjs)
  ⎿  Added 44 lines
      607  // ── Bible plane (Pass 4): book/chapter/verse nodes + CITES e
           dges ─────
      608  const bibleCounts = ingestBiblePlane(out);
      609  
      610 +// ── Pass 4b: imagery concepts × Bible verses (§7.7) ────────
          +─────────                                                     
      611 +// Pass 3a matched concepts before verse nodes existed; the im
          +agery layer                                                   
      612 +// additionally tags verses so meaning-first Scripture navigat
          +ion                                                           
      613 +// (CorpusDb.conceptVerseCounts / the Imagery Atlas) spans OT/
          +Psalms/NT.                                                    
      614 +// Keywords match at WORD boundaries here — verses are short, 
          +and substring                                                 
      615 +// hits on terse Latin keywords ("via" in "deviant") would pol
          +lute counts.                                                  
      616 +let verseInstanceEdges = 0;                                   
      617 +{                                                             
      618 +  const allVerses = out.prepare(                              
      619 +    `SELECT n.id, tb.latin, tb.english                        
      620 +     FROM nodes n JOIN text_blocks tb ON tb.node_id = n.id    
      621 +     WHERE n.kind = 'verse'`,                                 
      622 +  ).all();                                                    
      623 +  const verseNorm = allVerses.map((v) => ({                   
      624 +    id: Number(v.id),                                         
      625 +    padded: ` ${normalizeText(String(v.latin ?? ''))} ${normal
          +izeText(String(v.english ?? ''))} `,                          
      626 +  }));                                                        
      627 +  out.exec('BEGIN');                                          
      628 +  for (const concept of IMAGERY_CONCEPTS) {                   
      629 +    const cid = conceptNodeId.get(concept.id);                
      630 +    if (!cid) continue;                                       
      631 +    const kws = concept.keywords                              
      632 +      .map((k) => normalizeText(k))                           
      633 +      .filter(Boolean)                                        
      634 +      .map((k) => (k.includes(' ') ? k : ` ${k} `));          
      635 +    for (const v of verseNorm) {                              
      636 +      let matched = false;                                    
      637 +      for (const pat of concept.patterns) {                   
      638 +        if (pat.test(v.padded)) { matched = true; break; }    
      639 +      }                                                       
      640 +      if (!matched) {                                         
      641 +        for (const kw of kws) {                               
      642 +          if (v.padded.includes(kw)) { matched = true; break; 
          +}                                                             
      643 +        }                                                     
      644 +      }                                                       
      645 +      if (matched) {                                          
      646 +        insEdge.run(v.id, cid, 'INSTANCE_OF', 1.0, null);     
      647 +        verseInstanceEdges++;                                 
      648 +      }                                                       
      649 +    }                                                         
      650 +  }                                                           
      651 +  out.exec('COMMIT');                                         
      652 +}                                                             
      653 +                                                              
      654  // ── Interpretive layer (§7.7): vendored commentary → COMMENT
           S_ON edges ─
      655  // Guarded: an absent VENDORED source dir warns and skips; nev
           er breaks.
      656  let commentaryCounts = {};
● Update(scripts/ingest-corpus.mjs)
  ⎿  Added 1 line
      680    conceptBroaderEdges: broaderEdgeCount,
      681    autoConcepts: autoConceptCount,
      682    autoInstanceEdges: autoInstanceEdgeCount,
      683 +  verseInstanceEdges,                                         
      684    ...officeCounts,
      685    ...bibleCounts,
      686    ...commentaryCounts,

● Re-running the ingest with Pass 4b (background):

  Ran 1 shell command (ctrl+o to expand)

● Re-ingest running with the new verse-tagging pass. Meanwhile C1 (journal
  workspace components) is still building. Once both land: verify
  conceptVerseCounts returns real imagery counts, commit the ingest fix +
  regenerated db (LFS), then dispatch the final integration pass (C2: App
  rail/routes/context-menu capture + BN.1: Scripture Atlas).

✻ Cooked for 2m 19s

● Background command "Re-run ingest with verse-level imagery pass" completed
(exit code 0)

  Ran 1 shell command (ctrl+o to expand)

● 9,316 verse-level imagery edges landed, fill-log unchanged, suite 79/79.
  Verifying the query surface directly (my adapter call used a wrong export
  name):

  Ran 1 shell command (ctrl+o to expand)

● The Imagery Atlas data layer is live with plausible distributions. Committing
  the ingest fix + regenerated corpus (LFS), flipping BL.3/BM.3 to [X]:

● Update(CHECKLIST.md)
  ⎿  Added 1 line, removed 1 line
      521  ## Stanza B-L — Similarity UX
      522  - [X] **BL.1** `src/core/vector/clause.ts`: `Clause`, `splitCl
           auses`, `bestClause` per entity row + `tests/clause.test.ts` (
           determinism, min-length, argmax). _2026-07-14: linear-scan spl
           itter, exact offsets, 7 tests; suite 62/62._
      523  - [X] **BL.2** `src/ui/SimilarityGlyph.tsx` + MeaningPanel int
           egration: every similar-hit renders glyph + best-clause emphas
           is (rest behind "more"); score → tooltip. _2026-07-14: golden-
           angle deterministic layout; both grouped-similar call sites + 
           ConceptTag thread query/siblings._
      524 -- [/] **BL.3** `IMAGERY_CONCEPTS` (~15, §7.7 list) merged into
          - `src/core/ontology/concepts.ts`; shared re-ingest with BM.3 (
          -one `assets/missal.db` regeneration; fill-log delta must be ze
          -ro). _2026-07-14: 15 concepts + `sacred_imagery` parent append
          -ed, flow into CONCEPTS verified (46 ids); re-ingest pending Wa
          -ve B._                                                        
      524 +- [X] **BL.3** `IMAGERY_CONCEPTS` (~15, §7.7 list) merged into
          + `src/core/ontology/concepts.ts`; shared re-ingest with BM.3 (
          +one `assets/missal.db` regeneration; fill-log delta must be ze
          +ro). _2026-07-14: re-ingest run twice — orchestrator added Pas
          +s 4b (imagery × verses, word-boundary keywords) after finding 
          +Pass 3a predates verse nodes; 9,316 verse INSTANCE_OF edges (k
          +ing_kingdom 2791 … desert_exile 415); fill-log delta zero; sui
          +te 79/79._                                                    
      525  
      526  ## Stanza B-M — Interpretive layer (PD vendoring + commentary 
           ingest)
      527  - [X] **BM.1** Vendor `VENDORED/haydock/` (fulfils BF.1): clon
           e-at-home, PROVENANCE.md (source URL + commit/edition + date +
            license note) BEFORE assimilation; additive only. _2026-07-14
           : 1883 Dunigan expanded edition (USFM, complete 73-book canon,
            20,965 records) from cmahte/ENG-B-Haydock1883-pd-PSFM@0332c84
           ; .git removed post-lock._

● Update(CHECKLIST.md)
  ⎿  Added 1 line, removed 1 line
      526  ## Stanza B-M — Interpretive layer (PD vendoring + commentary 
           ingest)
      527  - [X] **BM.1** Vendor `VENDORED/haydock/` (fulfils BF.1): clon
           e-at-home, PROVENANCE.md (source URL + commit/edition + date +
            license note) BEFORE assimilation; additive only. _2026-07-14
           : 1883 Dunigan expanded edition (USFM, complete 73-book canon,
            20,965 records) from cmahte/ENG-B-Haydock1883-pd-PSFM@0332c84
           ; .git removed post-lock._
      528  - [X] **BM.2** Vendor `VENDORED/catena-aurea/` (Newman tr., PD
           ): same regime. _2026-07-14: Newman/Parker 1841–45 via tj06man
           /catena-aurea-project@d3829bb (wording verified); 816 pericope
            blocks, 4 Gospels._
      529 -- [/] **BM.3** `scripts/ingest-commentary.mjs` (`COMMENTARY_SO
          -URCES`, wired into `ingest-corpus.mjs`): `commentary:<source>/
          -<Book>/<ch>/<v>` nodes, English text_blocks, `COMMENTS_ON` edg
          -es, embeddings + FTS; `CorpusDb.commentaryFor` + adapter mirro
          -r; run shared re-ingest (with BL.3); fill-log delta zero; LFS 
          -push. _2026-07-14: script + query surface shipped, scratch run
          - 20,915 nodes / 24,244 edges; SHARED RE-INGEST PENDING. Findin
          -g: pre-existing DR JSON gap (Num 36, Jos 23–24, Judg 18–21, Ru
          -th 3–4, Judith 12–16, Esth 11+, Eccli 49–51, …) strands 865 Ha
          -ydock records as skips — complete the DR source in a BA follow
          --up._                                                         
      529 +- [X] **BM.3** `scripts/ingest-commentary.mjs` (`COMMENTARY_SO
          +URCES`, wired into `ingest-corpus.mjs`): `commentary:<source>/
          +<Book>/<ch>/<v>` nodes, English text_blocks, `COMMENTS_ON` edg
          +es, embeddings + FTS; `CorpusDb.commentaryFor` + adapter mirro
          +r; run shared re-ingest (with BL.3); fill-log delta zero; LFS 
          +push. _2026-07-14: shared re-ingest done — 20,915 commentary n
          +odes / 24,244 COMMENTS_ON edges in the committed db (139.5→~19
          +3 MB); fill-log delta zero. Finding: pre-existing DR JSON gap 
          +(Num 36, Jos 23–24, Judg 18–21, Ruth 3–4, Judith 12–16, Esth 1
          +1+, Eccli 49–51, …) strands 865 Haydock records as skips — com
          +plete the DR source in a BA follow-up._                       
      530  - [X] **BM.4** `src/core/ontology/parallels.ts`: `Pericope`/`P
           ERICOPES` (~60 spine, cross-checked against vendored Catena) +
            `SCENARIO_CLUSTERS`; `tests/parallels.test.ts` (refs parse, b
           ooks exist in BOOK_MAP). _2026-07-14: 79 pericopes / 8 cluster
           s, refs keyed Matt/Marc/Luc/Joann (BOOK_MAP canonical; entity 
           row reconciled), 152 refs machine-verified vs vendored DR; sui
           te 79/79._
      531  
      532  ## Stanza B-N — Scripture Atlas

  Ran 1 shell command (ctrl+o to expand)

● The corpus commit + LFS push (193 MB to Forgejo, plus the code-only mirror) is
  running in the background. Waiting on that push and the C1 journal-workspace
  agent; the final integration pass (C2 + Scripture Atlas) dispatches as soon as
  C1 hands back.

✻ Worked for 59s

● Background command "Commit re-ingest and push (LFS db to forgejo)" completed
(exit code 0)

  Ran 1 shell command (ctrl+o to expand)

● Corpus re-ingest pushed to both remotes (adc20316 — the 193 MB db as an LFS
  object on Forgejo, pointer-only on the GitHub mirror).

  Current scoreboard: BJ.1–3, BK.1–2, BL.1–3, BM.1–4, BC.1/2/4 all [X] — the
  theme, mobile interleave, similarity overhaul, and interpretive layer are
  implemented and committed. Still in flight: C1 (journal workspace components).
  Then the closing pass: C2 integration (App rail "Journal & Homilies", #/acc/
  route, ThemePicker mount, context-menu capture/highlight in both readers) and
  BN.1 (Scripture Atlas over the now-populated imagery/parallels/citation data).

✻ Sautéed for 17s

● Agent "C1: journal workspace components" failed: Agent terminated early due to
an API error: You've hit your session limit · resets 3am (America/Toronto)
  ⎿  You've hit your session limit · resets 3am (America/Toronto)

✻ Brewed for 53m 33s

❯ continue--quota reset                                                         

● Quota's back — picking up where C1 died. First, assessing what it left behind:

  Ran 1 shell command (ctrl+o to expand)
  ⎿  Agent "a331fc4c0278eb45c" had no active task; resumed from transcript 
     in the background with your message. You'll be notified when it 
     finishes. Output: 
     /tmp/claude-1000/-home-robin-CascadeProjects-StAndroidsMissal/8892cffc
     -2861-435f-85cc-683a9d6b646c/tasks/a331fc4c0278eb45c.output  ⎿  Interrupted · What should Claude do instead?

❯ wait you're at 86% quota for fable. Can  you annotate for glm-5.2 tgo tgake   
  over your role                                                                

● Write(LOGS/handoff-v05-14jul2026.md)
  ⎿  Wrote 40 lines to LOGS/handoff-v05-14jul2026.md
      1 # Orchestrator handoff — v0.5 wave (2026-07-14, Fable→GLM-5.2)
      3 **You are the orchestrator for the v0.5 presentation & meaning pl
        on) + `CHECKLIST.md` "v0.5 wave" stanzas B-J…B-O. Operator approv
        ed the full plan (all six workstreams); execution is mid-flight. 
         is the truth); everything below was true at handoff time — **ver
        t was still running when this handoff was written.
      5 ## Done, committed, pushed (both remotes; HEAD `adc20316` at hand
        off)
      7 | Commit | Content | Markers |
      9 | `
     … +106 lines (ctrl+o to expand)

  Pushed to master (ctrl+o to expand)

● Handoff written, committed, and pushed to both remotes (9e148f02,
  LOGS/handoff-v05-14jul2026.md).

  For the GLM-5.2 takeover, point the seat at the handoff, e.g.:

  opencode run -m zai-coding-plan/glm-5.2 "You are taking over as orchestrator
  for StAndroidsMissal (cwd /home/robin/CascadeProjects/StAndroidsMissal). Read
  LOGS/handoff-v05-14jul2026.md first, verify disk state against it, then
  continue the v0.5 wave per CHECKLIST.md stanzas B-J..B-O."

  Two things the successor must know (both are in the handoff doc, flagged
  loudly):

  1. The C1 subagent is still running in this session's background finishing
  JournalSidecar.tsx / JournalView.tsx / HomilyPlanner.tsx. If this session gets
  closed, whatever it wrote stays on disk — the handoff's first instruction is
  "check whether those three files exist and pass tsc/tests before deciding
  whether to verify-and-commit or re-author."
  2. The working tree has pre-existing dirt (icons, version stamps, computus.ts,
  etc.) that predates this wave — scoped adds only, never git add -A.

  State at handoff: 7 commits pushed, 15 of the wave's 19 checklist tasks [X]
  (theme + interleave + similarity + interpretive layer + sidecar core all done;
  suite 79/79, tsc and build clean); remaining are the C1 components, BO.1
  capture menus, BO.3 App integration, BN.1 Scripture Atlas, and the end-to-end
  browser verification pass. RevenueCat is seeded with the contract vocabulary
  and the MCP server is registered user-scope, so the successor (and any
  Devin-side session) reconciles to those lookup keys.

✻ Brewed for 1m 29s

❯ HMM that's 86% until the 16th; 14% until next 5hr quota--can you tie up       
  whatever is most messy but leave some safety quota                            

● Checking the C1 agent's status first — it's both the messiest loose end and
  the main quota drain:

  Listed 1 directory (ctrl+o to expand)

● Stop Task
  ⎿  C1: journal workspace components · stopped

  Ran 1 shell command (ctrl+o to expand)

● Agent "C1: journal workspace components" was stopped by Claude

❯ is codegraph sync automaticlaly firing perioidcally?                          

  Searched for 1 pattern (ctrl+o to expand)

● Update(CHECKLIST.md)
  ⎿  Added 1 line, removed 1 line
      476  ## Stanza B-C — Accompaniment model + sidecar v2 + editor
      477  - [X] **BC.1** `src/core/accompaniment/types.ts` (`Accompanime
           nt`, `OccurrenceSelector`, `Exposure`) + `store.ts` (`SIDECAR_
           SCHEMA_SQL_V2`, `SidecarDb` v2, platform byte-persistence web 
           OPFS/IndexedDB + Tauri `load_sidecar`/`save_sidecar`); annotat
           ions-localStorage migration (old key read-only). _2026-07-14: 
           +`quote_alt` col (§7.7 dual-pane highlight) w/ ensureColumn up
           grade; migration reads both `sam.annotations.v1` and the store
           's real `standroidsmissal.annotations.v1`; Rust cmds cargo-che
           ck clean._
      478  - [X] **BC.2** `resolve.ts`: `accompanimentsForDay`/`forAnchor
           `/`matchesSelector` over computus + recurrence rules. _2026-07
           -14: recurrence grammar `weekly:<0-6>` / `nth-weekday:<n>:<0-6
           >`._
      479 -- [ ] **BC.3** `AccompanimentEditor.tsx` (TipTap; body_pm + bo
          -dy_html snapshot).                                            
      479 +- [X] **BC.3** `AccompanimentEditor.tsx` (TipTap; body_pm + bo
          +dy_html snapshot). _2026-07-14: @tiptap/react+starter-kit ^3.2
          +7.4; tags→theme selectors; debounced persist._                
      480  - [X] **BC.4** `tests/accompaniment.test.ts`: selector resolut
           ion incl. moveable feasts across year boundaries; migration; f
           ree-form theme tags. _2026-07-14: 10 tests incl. Pasc0-2 2026↔
           2027 boundary + first-Friday recurrence; suite 73/73._
      481  
      482  ## Stanza B-D — Exposure surfaces

● Update(CHECKLIST.md)
  ⎿  Added 2 lines, removed 2 lines
      480  - [X] **BC.4** `tests/accompaniment.test.ts`: selector resolut
           ion incl. moveable feasts across year boundaries; migration; f
           ree-form theme tags. _2026-07-14: 10 tests incl. Pasc0-2 2026↔
           2027 boundary + first-Friday recurrence; suite 73/73._
      481  
      482  ## Stanza B-D — Exposure surfaces
      483 -- [ ] **BD.1** `JournalView` (date timeline; day-chip today's 
          -entries).                                                     
      484 -- [ ] **BD.2** `HomilyPlanner` (selector-projected planning ca
          -lendar; supersedes PlannerView/HomilyEditor rows).            
      483 +- [X] **BD.1** `JournalView` (date timeline; day-chip today's 
          +entries). _2026-07-14: component shipped; rail reachability la
          +nds with BO.3._                                               
      484 +- [X] **BD.2** `HomilyPlanner` (selector-projected planning ca
          +lendar; supersedes PlannerView/HomilyEditor rows). _2026-07-14
          +: month grid projects selectors via matchesSelector; "this Sun
          +day's drafts"; reachability lands with BO.3._                 
      485  - [ ] **BD.3** `StudyBuilder` (class centroid: recurrence grou
           p + anchors + materials; print stylesheet handouts).
      486  - [ ] **BD.4** `NewsletterDesk` behind `EntitlementGate('newsl
           etter-desk')` + `parish_profile` masthead (name/logo/letterhea
           d/colors/address); print + email-ready HTML export + share lin
           k.
      487  

● Update(CHECKLIST.md)
  ⎿  Added 1 line, removed 1 line
      534  
      535  ## Stanza B-O — Journal sidecar workspace (after B-C core)
      536  - [ ] **BO.1** Capture + highlight context actions: ReaderView
           /BibleView ctx-menus gain "✎ Add to Journal/Homily notes" (ope
           ns JournalSidecar with quote + `alignSelection` counterpart + 
           anchor) and "🖍 Highlight both panes" (lightweight accompanimen
           t, quote+quoteAlt through `mark.ann` in both panes).
      537 -- [ ] **BO.2** `src/ui/JournalSidecar.tsx` (`JournalSidecar` +
          - `ConnectionsPanel` per entity rows): source block, `Accompani
          -mentEditor` embed, connections cards (corpus vectors + own acc
          -ompaniments via `sidecar_embeddings` + commentary; why-bridge 
          -+ evidence chips + add/dismiss), destinations → exposure/selec
          -tors, toast.                                                  
      537 +- [X] **BO.2** `src/ui/JournalSidecar.tsx` (`JournalSidecar` +
          + `ConnectionsPanel` per entity rows): source block, `Accompani
          +mentEditor` embed, connections cards (corpus vectors + own acc
          +ompaniments via `sidecar_embeddings` + commentary; why-bridge 
          ++ evidence chips + add/dismiss), destinations → exposure/selec
          +tors, toast. _2026-07-14: 3-source connections (corpus/persona
          +l/vendored commentary); opens via BO.1 ctx-menu (pending)._   
      538  - [ ] **BO.3** Integration (App owner): `View` gains `'journal
           '`; NAV "Journal & Homilies" (✎); `#/acc/<id>` route resolves 
           (completes BB.3); ThemePicker mounted in rail; JournalView/Hom
           ilyPlanner (BD.1/BD.2) reachable from the journal view's tabs.

● Update(LOGS/handoff-v05-14jul2026.md)
  ⎿  Added 3 lines, removed 4 lines
      16  
      17  Suite at handoff: **79/79**, `npx tsc -b` clean, `npm run build
          ` clean.
      18  
      19 -## In flight at handoff (CHECK DISK FIRST)                     
      19 +## ~~In flight at handoff~~ RESOLVED before Fable signed off   
      20  
      21 -A subagent ("C1") was resumed in the dying Fable session to fin
         -ish the journal workspace components. Already on disk: TipTap `
         -^3.27.4` in package.json + `src/ui/AccompanimentEditor.tsx`. It
         - was writing: `src/ui/JournalSidecar.tsx` (+ exported `Connecti
         -onsPanel`), `src/ui/JournalView.tsx`, `src/ui/HomilyPlanner.tsx
         -`. **If those files exist and tsc/tests pass, verify semantical
         -ly against the P-T/P-S entity rows, flip BC.3/BO.2/BD.1/BD.2 co
         -mponent-halves, commit scoped (include package.json+package-loc
         -k TipTap lines — but check `git diff package.json` for unrelate
         -d pre-existing edits first). If absent/partial, author them you
         -rself per stanza B-O + the entity rows + `DOCS/standroids-journ
         -al-sidecar-standalone.html` + the PRD.**                       
      21 +C1 completed: `AccompanimentEditor.tsx`, `JournalSidecar.tsx` (
         ++`ConnectionsPanel`), `JournalView.tsx`, `HomilyPlanner.tsx` al
         +l on disk, verified (tsc clean, suite 79/79), committed with th
         +e TipTap deps — BC.3/BO.2/BD.1/BD.2 flipped `[X]`. NOTE: that c
         +ommit's package.json also carries the PRE-EXISTING stranded ver
         +sion-stamp (1.15.31005) + build-entrypoint script lines from a 
         +prior session (they match CLAUDE.md's documented entrypoints; c
         +ommitting them un-strands TC10 WIP — labeled in the commit mess
         +age).                                                          
      22  
      23  ## Remaining work (in order)
      24  
      25 -1. **C1 remainder** (above).                                   
      26 -2. **BO.1** — ReaderView + BibleView ctx-menus: "✎ Add to Journ
         -al/Homily notes" (opens JournalSidecar with quote + `alignSelec
         -tion` counterpart + anchor nodeKey) and "🖍 Highlight both panes
         -" (lightweight accompaniment quote+quoteAlt → renders via exist
         -ing `mark.ann` in both panes; `quote_alt` column exists in side
         -car v2).                                                       
      25 +1. **BO.1** — ReaderView + BibleView ctx-menus: "✎ Add to Journ
         +al/Homily notes" (opens JournalSidecar with quote + `alignSelec
         +tion` counterpart + anchor nodeKey) and "🖍 Highlight both panes
         +" (lightweight accompaniment quote+quoteAlt → renders via exist
         +ing `mark.ann` in both panes; `quote_alt` column exists in side
         +car v2).                                                       
      26  3. **BO.3** — App integration (single owner of App.tsx): `View`
           + `'journal'`; NAV entry "Journal & Homilies" (✎); open Sideca
          rDb at App level (`SidecarDb.open()` + `migrateLocalStorageAnno
          tations`) and thread `sidecar` to views; mount `ThemePicker` in
           the rail (init theme on boot from settings); `#/acc/<id>` hash
           route (completes BB.3 — see `parseHashRoute` in `src/core/shar
          e/shareLink.ts`); JournalView/HomilyPlanner as tabs of the jour
          nal view; JournalSidecar opens via the same `.split` layout Mea
          ningPanel uses (`action` state gains a `capture` variant or a p
          arallel state — architect's choice, document it).
      27  4. **BN.1** — `src/ui/ScriptureAtlas.tsx` (`AtlasMode = 'canoni
          cal'|'imagery'|'parallels'`) + BibleView mode switch + commenta
          ry read-only layer beneath verses. Data is ALL live in the comm
          itted db: `CorpusDb.conceptVerseCounts()` (imagery counts, e.g.
           king_kingdom 2791), `chapterCiteCounts(book)`, `commentaryFor(
          book, ch, verse?)`, `PERICOPES`/`SCENARIO_CLUSTERS` in `src/cor
          e/ontology/parallels.ts` (Gospel keys `Matt/Marc/Luc/Joann`). C
          SS classes `.atlas-*` already in styles.css. Differential font 
          sizing: `font-size ∝ √count` inline, weight ∝ CITES.
      28  5. **Verification pass** — `npm test`, `npx tsc -b`, `npm run b
          uild`, then `npm run dev` browser pass: theme toggle (skeuomorp
          hic/sanctissimissa × light/dark × seasonal colors), ≤1100px int
          erleave with dual-language selection highlight, capture→edit→co
          nnect→destination round-trip persisting across reload (IndexedD
          B `standroidsmissal`/`blobs`/`sidecar.db`), MeaningPanel glyph+
          clause hits, Atlas modes click-through to `#/verse/…`, commenta
          ry under verses with source attribution.

  Pushed to master (ctrl+o to expand)

● All tied up — everything is committed and pushed (8c2ce4f1, both remotes), and
  I'm stopping here to preserve your safety margin.

  On your codegraph question: no, it's not periodic — it's event-driven. Two
  mechanisms keep it fresh: the cclaude/zclaude launch wrapper runs codegraph 
  sync once at session start, and during the session the codegraph serve --mcp
  file-watcher re-indexes incrementally on every write with a ~0.5–2 s debounce.
  Since the codegraph MCP server was connected and answering in this session,
  the watcher is live — today's new files (BilingualText, JournalSidecar, the
  accompaniment core, etc.) were indexed as the agents wrote them. Nothing fires
  on a timer, and nothing needed from you.

  What got tied up in this final stretch:
  - The interrupted C1 agent's work was actually complete on disk — I stopped
  the agent (saving its verification/reporting tokens), verified myself (tsc
  clean, suite 79/79), and committed all four journal components:
  AccompanimentEditor (TipTap), JournalSidecar+ConnectionsPanel, JournalView,
  HomilyPlanner → BC.3, BO.2, BD.1, BD.2 all [X].
  - That commit also un-strands the prior session's uncommitted package.json
  version-stamp/build-entrypoint WIP (it matches CLAUDE.md's documented
  entrypoints; labeled honestly in the commit message).
  - The handoff doc (LOGS/handoff-v05-14jul2026.md) is updated: C1 is marked
  resolved, so the GLM-5.2 successor's list is now exactly three items — BO.1
  (context-menu capture + highlight-both-panes in the two readers), BO.3 (App
  integration: rail entry, #/acc/ route, sidecar open + migration, ThemePicker
  mount), BN.1 (Scripture Atlas — all its data is already live in the committed
  db), then the end-to-end browser verification pass.

  Wave scoreboard: 18 of 21 v0.5 tasks [X], 9 commits pushed, suite green
  throughout, fill-log delta zero, corpus (193 MB, LFS on Forgejo only) carrying
  20,915 commentary nodes and 9,316 verse-level imagery edges. The takeover
  command is in my previous message; the handoff file is the successor's first
  read.
✻ Brewed for 4m 4s